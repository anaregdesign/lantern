// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: graph/v1/graph.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import Long from "long";
import { Duration } from "../../google/protobuf/duration";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "graph.v1";

export enum Optimization {
  OPTIMIZATION_UNSPECIFIED = 0,
  OPTIMIZATION_MINIMUM_SPANNING_TREE = 1,
  OPTIMIZATION_MAXIMUM_SPANNING_TREE = 2,
  OPTIMIZATION_SHORTEST_PATH_TREE = 3,
  OPTIMIZATION_SHORTEST_PATH_TREE_INVERSE = 4,
  UNRECOGNIZED = -1,
}

export function optimizationFromJSON(object: any): Optimization {
  switch (object) {
    case 0:
    case "OPTIMIZATION_UNSPECIFIED":
      return Optimization.OPTIMIZATION_UNSPECIFIED;
    case 1:
    case "OPTIMIZATION_MINIMUM_SPANNING_TREE":
      return Optimization.OPTIMIZATION_MINIMUM_SPANNING_TREE;
    case 2:
    case "OPTIMIZATION_MAXIMUM_SPANNING_TREE":
      return Optimization.OPTIMIZATION_MAXIMUM_SPANNING_TREE;
    case 3:
    case "OPTIMIZATION_SHORTEST_PATH_TREE":
      return Optimization.OPTIMIZATION_SHORTEST_PATH_TREE;
    case 4:
    case "OPTIMIZATION_SHORTEST_PATH_TREE_INVERSE":
      return Optimization.OPTIMIZATION_SHORTEST_PATH_TREE_INVERSE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Optimization.UNRECOGNIZED;
  }
}

export function optimizationToJSON(object: Optimization): string {
  switch (object) {
    case Optimization.OPTIMIZATION_UNSPECIFIED:
      return "OPTIMIZATION_UNSPECIFIED";
    case Optimization.OPTIMIZATION_MINIMUM_SPANNING_TREE:
      return "OPTIMIZATION_MINIMUM_SPANNING_TREE";
    case Optimization.OPTIMIZATION_MAXIMUM_SPANNING_TREE:
      return "OPTIMIZATION_MAXIMUM_SPANNING_TREE";
    case Optimization.OPTIMIZATION_SHORTEST_PATH_TREE:
      return "OPTIMIZATION_SHORTEST_PATH_TREE";
    case Optimization.OPTIMIZATION_SHORTEST_PATH_TREE_INVERSE:
      return "OPTIMIZATION_SHORTEST_PATH_TREE_INVERSE";
    case Optimization.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Vertex {
  key: string;
  expiration: Date | undefined;
  float64?: number | undefined;
  float32?: number | undefined;
  int32?: number | undefined;
  int64?: Long | undefined;
  uint32?: number | undefined;
  uint64?: Long | undefined;
  bool?: boolean | undefined;
  string?: string | undefined;
  bytes?: Buffer | undefined;
  timestamp?: Date | undefined;
  duration?:
    | Duration
    | undefined;
  /**
   * nil signals that the vertex carries no value (an "existence-only"
   * marker). The bool itself is always true when present; the variant
   * exists so the oneof can distinguish "explicitly nil" from "unset".
   */
  nil?: boolean | undefined;
}

export interface Edge {
  tail: string;
  head: string;
  weight: number;
  expiration: Date | undefined;
}

export interface Graph {
  vertices: Vertex[];
  edges: Edge[];
}

export interface IlluminateRequest {
  seed: string;
  step: number;
  k: number;
  tfidf: boolean;
  optimization: Optimization;
}

export interface IlluminateResponse {
  graph: Graph | undefined;
}

export interface GetVertexRequest {
  key: string;
}

export interface GetVertexResponse {
  vertex: Vertex | undefined;
}

/**
 * GetVerticesRequest reads several vertices in one round trip. Subject to
 * the same MaxBatchSize / MaxKeyLen guard rails as the write RPCs.
 */
export interface GetVerticesRequest {
  keys: string[];
}

export interface GetVerticesResponse {
  /**
   * vertices contains every key that was present at the time of the call.
   * Order is unspecified; clients should match by Vertex.key.
   */
  vertices: Vertex[];
  /** missing lists the requested keys that did not exist (or had expired). */
  missing: string[];
}

/**
 * PutVertexRequest writes a single vertex with upsert semantics. The
 * Vertex.key field selects the target row and Vertex.expiration controls
 * the TTL (absolute time). This is the singular convenience wrapper over
 * PutVertices and shares its guard rails.
 */
export interface PutVertexRequest {
  vertex: Vertex | undefined;
}

export interface PutVertexResponse {
}

/**
 * PutVerticesRequest writes vertices with upsert semantics: each Vertex.key
 * replaces any existing value at that key. Use the Vertex.expiration field
 * (absolute time) to control TTL.
 */
export interface PutVerticesRequest {
  vertices: Vertex[];
}

export interface PutVerticesResponse {
  /**
   * Number of vertices accepted by the server. Currently always equals the
   * request size on success; reserved for future per-item validation that may
   * report partial writes.
   */
  written: number;
}

/**
 * DeleteVertexRequest removes the vertex at `key`. Edges incident to the
 * removed vertex are NOT eagerly cascaded: the periodic GC loop reaps
 * orphaned (tail, head) rows on its next tick, and reads against missing
 * endpoints return NotFound in the meantime.
 */
export interface DeleteVertexRequest {
  key: string;
}

export interface DeleteVertexResponse {
  /** existed is true if the vertex was present and removed by this call. */
  existed: boolean;
}

/**
 * DeleteVerticesRequest removes several vertices in one round trip. Same
 * cascade semantics as DeleteVertex (edges reaped lazily by the GC loop).
 * Subject to the same MaxBatchSize / MaxKeyLen guard rails as the put RPCs.
 */
export interface DeleteVerticesRequest {
  keys: string[];
}

export interface DeleteVerticesResponse {
  /** Number of keys the server attempted to delete (equals len(keys) on success). */
  deleted: number;
}

/**
 * ScanVerticesRequest streams vertices whose key starts with `prefix` in
 * lexicographic order. `limit` caps the number returned in one call; the
 * server enforces a default when `limit == 0` and a hard maximum (see
 * RateLimitConfig / ScanConfig on the server). `cursor` MUST be treated as
 * opaque bytes by clients — pass back exactly what the previous response
 * returned in `next_cursor`. An empty `cursor` starts from the beginning.
 */
export interface ScanVerticesRequest {
  prefix: string;
  limit: number;
  /**
   * NOTE: opaque to the caller; not interchangeable with cursors from
   * other Scan* RPCs (e.g. ScanEdges). Cross-feeding is rejected with
   * INVALID_ARGUMENT rather than silently restarting the scan.
   */
  cursor: Buffer;
}

export interface ScanVerticesResponse {
  /**
   * vertices returned in ascending key order. May be shorter than `limit`
   * when the underlying range is exhausted.
   */
  vertices: Vertex[];
  /**
   * next_cursor is non-empty when more results are available. An empty
   * value signals end of stream.
   */
  nextCursor: Buffer;
}

/**
 * CountVerticesByPrefixRequest returns the number of live vertex keys with
 * the given prefix. Cheap (radix-only) and not subject to `limit`.
 */
export interface CountVerticesByPrefixRequest {
  prefix: string;
}

export interface CountVerticesByPrefixResponse {
  count: Long;
}

/**
 * DeleteVerticesByPrefixRequest deletes up to `limit` vertices whose key
 * starts with `prefix`. `limit == 0` lets the server apply its configured
 * default (see RateLimitConfig / ScanConfig). When `dry_run` is true, no
 * deletion is performed and the response reports the number that *would*
 * be deleted.
 */
export interface DeleteVerticesByPrefixRequest {
  prefix: string;
  limit: number;
  dryRun: boolean;
}

export interface DeleteVerticesByPrefixResponse {
  deleted: Long;
}

export interface GetEdgeRequest {
  tail: string;
  head: string;
}

export interface GetEdgeResponse {
  edge: Edge | undefined;
}

/**
 * GetEdgesRequest reads several edges in one round trip. Subject to the
 * same MaxBatchSize / MaxKeyLen guard rails as the write RPCs.
 */
export interface GetEdgesRequest {
  edges: EdgeKey[];
}

export interface GetEdgesResponse {
  /**
   * edges contains every (tail, head) pair that was present at the time of
   * the call. Order is unspecified; clients should match by (Edge.tail,
   * Edge.head).
   */
  edges: Edge[];
  /**
   * missing lists the requested (tail, head) pairs that did not exist (or
   * had expired).
   */
  missing: EdgeKey[];
}

export interface DeleteEdgeRequest {
  tail: string;
  head: string;
}

export interface DeleteEdgeResponse {
  /** existed is true if the edge was present and removed by this call. */
  existed: boolean;
}

/** EdgeKey identifies an edge by its (tail, head) pair without weight. */
export interface EdgeKey {
  tail: string;
  head: string;
}

/**
 * ScanEdgesRequest streams edges whose tail key starts with `tail_prefix`
 * AND whose head key starts with `head_prefix`, in ascending (tail, head)
 * order. Either prefix may be empty to disable the corresponding filter
 * (both empty scans every edge). `limit` caps the number returned in one
 * call; the server enforces a default when `limit == 0` and a hard maximum
 * (see ScanConfig on the server). `cursor` MUST be treated as opaque bytes
 * by clients — pass back exactly what the previous response returned in
 * `next_cursor`. An empty `cursor` starts from the beginning.
 *
 * Implementation note: v1 walks the tail-side prefix index and applies
 * `head_prefix` as a post-filter; for highly selective `head_prefix`
 * queries the server may visit many tails to fill one page. Latency is
 * reported in the standard scan histogram so operators can spot
 * pathological filters.
 */
export interface ScanEdgesRequest {
  tailPrefix: string;
  headPrefix: string;
  limit: number;
  /**
   * NOTE: opaque to the caller; not interchangeable with cursors from
   * other Scan* RPCs (e.g. ScanVertices). Cross-feeding is rejected with
   * INVALID_ARGUMENT rather than silently restarting the scan.
   */
  cursor: Buffer;
}

export interface ScanEdgesResponse {
  /**
   * edges returned in ascending (tail, head) order. May be shorter than
   * `limit` when the underlying range is exhausted.
   */
  edges: Edge[];
  /**
   * next_cursor is non-empty when more results are available. An empty
   * value signals end of stream.
   */
  nextCursor: Buffer;
}

/**
 * DeleteEdgesRequest removes several edges in one round trip. Subject to the
 * MaxBatchSize / MaxKeyLen guard rails.
 */
export interface DeleteEdgesRequest {
  edges: EdgeKey[];
}

export interface DeleteEdgesResponse {
  /** Number of edges the server attempted to delete (equals len(edges) on success). */
  deleted: number;
}

/**
 * AddEdgeRequest accumulates weight onto a single (tail, head) pair: repeated
 * calls with the same endpoints sum their weights. This is the singular
 * convenience wrapper over AddEdges and shares its non-idempotent semantics.
 */
export interface AddEdgeRequest {
  edge: Edge | undefined;
}

export interface AddEdgeResponse {
}

/**
 * AddEdgesRequest accumulates weight onto each (tail, head) pair: repeated
 * calls with the same endpoints sum their weights. This operation is
 * non-idempotent.
 */
export interface AddEdgesRequest {
  edges: Edge[];
}

export interface AddEdgesResponse {
  /** Number of edges whose weight contributions were accepted. */
  written: number;
}

/**
 * PutEdgeRequest overwrites a single (tail, head) pair, replacing any
 * existing weight and expiration. This is the singular convenience wrapper
 * over PutEdges and shares its idempotent semantics.
 */
export interface PutEdgeRequest {
  edge: Edge | undefined;
}

export interface PutEdgeResponse {
}

/**
 * PutEdgesRequest overwrites each (tail, head) pair, replacing any existing
 * weight and expiration. This operation is idempotent.
 */
export interface PutEdgesRequest {
  edges: Edge[];
}

export interface PutEdgesResponse {
  /** Number of edges accepted (overwritten or created). */
  written: number;
}

/**
 * GetServerStatusRequest carries no parameters — the response is a flat
 * snapshot of the server's identity, build, configuration ceilings, and
 * current live counts. Intended for the admin UI's "Ops" tab and any
 * lightweight smoke-test tooling that just needs to confirm "the server
 * is up and roughly configured as expected".
 */
export interface GetServerStatusRequest {
}

export interface GetServerStatusResponse {
  /**
   * Build/version stamp. Falls back to "dev" when the binary was built
   * without VCS info or without LANTERN_VERSION set.
   */
  version: string;
  /** Reports `runtime.Version()` (e.g. "go1.26.4"). */
  goVersion: string;
  /**
   * Wall-clock instant the server process started serving requests.
   * Captured at gRPC server start, not at process start, so the value
   * reflects "ready to serve" rather than "wire init done".
   */
  startedAt:
    | Date
    | undefined;
  /**
   * Convenience field: now - started_at on the server side, so clients
   * do not have to know the server's clock to display uptime.
   */
  uptime:
    | Duration
    | undefined;
  /**
   * The default TTL applied to vertices/edges when the caller does not
   * specify Expiration (LANTERN_DEFAULT_TTL_SECONDS).
   */
  defaultTtl:
    | Duration
    | undefined;
  /** Validation ceiling for batch RPCs (LANTERN_MAX_BATCH_SIZE). */
  maxBatchSize: number;
  /** Validation ceiling for vertex/edge keys (LANTERN_MAX_KEY_BYTES). */
  maxKeyBytes: number;
  /**
   * Per-call defaults / hard caps for prefix-scan pagination
   * (LANTERN_SCAN_DEFAULT_LIMIT / LANTERN_SCAN_MAX_LIMIT).
   */
  scanDefaultLimit: number;
  scanMaxLimit: number;
  /**
   * True when the gRPC server is terminating TLS
   * (LANTERN_TLS_CERT_FILE + LANTERN_TLS_KEY_FILE both set).
   */
  tlsEnabled: boolean;
  /**
   * True when this server is wired to a mutation log + HLC clock and is
   * therefore a member of a replication group. False on single-node
   * deployments.
   */
  replicationEnabled: boolean;
  /**
   * Live counts pulled from the in-memory graph cache. Cheap to compute
   * (index sizes, no scan). Intended for at-a-glance dashboards — these
   * are not transactional snapshots and may include not-yet-collected
   * expired entries bounded by the GC tick.
   */
  vertexCount: Long;
  edgeCount: Long;
}

/**
 * ReplicationPeer is one row of the GetReplicationStatus snapshot.
 * Each row models the local node's view of a single outbound peer
 * connection owned by the replication pump (#185). Fields are
 * best-effort point-in-time samples; clients should treat consecutive
 * snapshots as monotonically advancing rather than transactional.
 */
export interface ReplicationPeer {
  /**
   * address is the dial target as configured in LANTERN_PEERS (or
   * resolved from DNS discovery). Stable across reconnects for a given
   * peer, so it doubles as the row identity for admin UI displays.
   */
  address: string;
  state: ReplicationPeer_State;
  /**
   * last_event_at is the wall-clock instant the pump last received any
   * frame (Subscribe or Snapshot) from this peer. Unset when nothing
   * has been received yet. Combined with the response-level local_now
   * it yields the "how stale is this stream" diagnostic the admin UI
   * shows as a lag bar.
   */
  lastEventAt:
    | Date
    | undefined;
  /**
   * applied_seq is the highest peer-local sequence number the pump has
   * successfully consumed from this peer. Zero on a fresh connection.
   */
  appliedSeq: Long;
  /**
   * error carries the last non-recoverable error message reported by
   * the per-peer session loop. Cleared on the next successful frame.
   */
  error: string;
}

export enum ReplicationPeer_State {
  STATE_UNSPECIFIED = 0,
  /**
   * STATE_CONNECTING - Pump is dialing or has dialed but Subscribe has not yet
   * returned a frame.
   */
  STATE_CONNECTING = 1,
  /** STATE_STREAMING - Subscribe stream is open and frames have been received. */
  STATE_STREAMING = 2,
  /**
   * STATE_BACKOFF - Last session errored; the pump is sleeping before the next
   * reconnect attempt.
   */
  STATE_BACKOFF = 3,
  /**
   * STATE_CLOSED - Per-peer goroutine has exited (typically because DNS discovery
   * removed the address). Reported transiently — the row drops out
   * of subsequent snapshots once the supervisor has reaped it.
   */
  STATE_CLOSED = 4,
  UNRECOGNIZED = -1,
}

export function replicationPeer_StateFromJSON(object: any): ReplicationPeer_State {
  switch (object) {
    case 0:
    case "STATE_UNSPECIFIED":
      return ReplicationPeer_State.STATE_UNSPECIFIED;
    case 1:
    case "STATE_CONNECTING":
      return ReplicationPeer_State.STATE_CONNECTING;
    case 2:
    case "STATE_STREAMING":
      return ReplicationPeer_State.STATE_STREAMING;
    case 3:
    case "STATE_BACKOFF":
      return ReplicationPeer_State.STATE_BACKOFF;
    case 4:
    case "STATE_CLOSED":
      return ReplicationPeer_State.STATE_CLOSED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReplicationPeer_State.UNRECOGNIZED;
  }
}

export function replicationPeer_StateToJSON(object: ReplicationPeer_State): string {
  switch (object) {
    case ReplicationPeer_State.STATE_UNSPECIFIED:
      return "STATE_UNSPECIFIED";
    case ReplicationPeer_State.STATE_CONNECTING:
      return "STATE_CONNECTING";
    case ReplicationPeer_State.STATE_STREAMING:
      return "STATE_STREAMING";
    case ReplicationPeer_State.STATE_BACKOFF:
      return "STATE_BACKOFF";
    case ReplicationPeer_State.STATE_CLOSED:
      return "STATE_CLOSED";
    case ReplicationPeer_State.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * GetReplicationStatusRequest carries no parameters. The response is a
 * flat snapshot of the local node's view of every outbound peer.
 */
export interface GetReplicationStatusRequest {
}

export interface GetReplicationStatusResponse {
  /**
   * node_id is the local HLC NodeID rendered as lowercase hex (32 chars).
   * Stable for the lifetime of the process; either configured via
   * LANTERN_NODE_ID or randomly generated at startup.
   */
  nodeId: string;
  /**
   * local_now is the server's wall-clock at the moment the snapshot
   * was taken. Provided so clients can compute per-peer staleness
   * (local_now - last_event_at) without trusting their own clock.
   */
  localNow:
    | Date
    | undefined;
  /**
   * enabled is false on a single-instance deployment (no peers
   * configured AND no DNS discovery). When false, peers is empty but
   * the response is still well-formed — handlers never return
   * Unimplemented for this RPC.
   */
  enabled: boolean;
  /**
   * peers is the per-peer slice. Order is the supervisor's iteration
   * order and is intentionally unspecified; clients sort by address
   * for display.
   */
  peers: ReplicationPeer[];
}

function createBaseVertex(): Vertex {
  return {
    key: "",
    expiration: undefined,
    float64: undefined,
    float32: undefined,
    int32: undefined,
    int64: undefined,
    uint32: undefined,
    uint64: undefined,
    bool: undefined,
    string: undefined,
    bytes: undefined,
    timestamp: undefined,
    duration: undefined,
    nil: undefined,
  };
}

export const Vertex: MessageFns<Vertex> = {
  encode(message: Vertex, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.expiration !== undefined) {
      Timestamp.encode(toTimestamp(message.expiration), writer.uint32(18).fork()).join();
    }
    if (message.float64 !== undefined) {
      writer.uint32(81).double(message.float64);
    }
    if (message.float32 !== undefined) {
      writer.uint32(93).float(message.float32);
    }
    if (message.int32 !== undefined) {
      writer.uint32(96).int32(message.int32);
    }
    if (message.int64 !== undefined) {
      writer.uint32(104).int64(message.int64.toString());
    }
    if (message.uint32 !== undefined) {
      writer.uint32(112).uint32(message.uint32);
    }
    if (message.uint64 !== undefined) {
      writer.uint32(120).uint64(message.uint64.toString());
    }
    if (message.bool !== undefined) {
      writer.uint32(128).bool(message.bool);
    }
    if (message.string !== undefined) {
      writer.uint32(138).string(message.string);
    }
    if (message.bytes !== undefined) {
      writer.uint32(146).bytes(message.bytes);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(154).fork()).join();
    }
    if (message.duration !== undefined) {
      Duration.encode(message.duration, writer.uint32(162).fork()).join();
    }
    if (message.nil !== undefined) {
      writer.uint32(240).bool(message.nil);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Vertex {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVertex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expiration = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 81) {
            break;
          }

          message.float64 = reader.double();
          continue;
        }
        case 11: {
          if (tag !== 93) {
            break;
          }

          message.float32 = reader.float();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.int32 = reader.int32();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.int64 = Long.fromString(reader.int64().toString());
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.uint32 = reader.uint32();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.uint64 = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.bool = reader.bool();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.string = reader.string();
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.bytes = Buffer.from(reader.bytes());
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.duration = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 30: {
          if (tag !== 240) {
            break;
          }

          message.nil = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Vertex {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      expiration: isSet(object.expiration) ? fromJsonTimestamp(object.expiration) : undefined,
      float64: isSet(object.float64) ? globalThis.Number(object.float64) : undefined,
      float32: isSet(object.float32) ? globalThis.Number(object.float32) : undefined,
      int32: isSet(object.int32) ? globalThis.Number(object.int32) : undefined,
      int64: isSet(object.int64) ? Long.fromValue(object.int64) : undefined,
      uint32: isSet(object.uint32) ? globalThis.Number(object.uint32) : undefined,
      uint64: isSet(object.uint64) ? Long.fromValue(object.uint64) : undefined,
      bool: isSet(object.bool) ? globalThis.Boolean(object.bool) : undefined,
      string: isSet(object.string) ? globalThis.String(object.string) : undefined,
      bytes: isSet(object.bytes) ? Buffer.from(bytesFromBase64(object.bytes)) : undefined,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      duration: isSet(object.duration) ? Duration.fromJSON(object.duration) : undefined,
      nil: isSet(object.nil) ? globalThis.Boolean(object.nil) : undefined,
    };
  },

  toJSON(message: Vertex): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.expiration !== undefined) {
      obj.expiration = message.expiration.toISOString();
    }
    if (message.float64 !== undefined) {
      obj.float64 = message.float64;
    }
    if (message.float32 !== undefined) {
      obj.float32 = message.float32;
    }
    if (message.int32 !== undefined) {
      obj.int32 = Math.round(message.int32);
    }
    if (message.int64 !== undefined) {
      obj.int64 = (message.int64 || Long.ZERO).toString();
    }
    if (message.uint32 !== undefined) {
      obj.uint32 = Math.round(message.uint32);
    }
    if (message.uint64 !== undefined) {
      obj.uint64 = (message.uint64 || Long.UZERO).toString();
    }
    if (message.bool !== undefined) {
      obj.bool = message.bool;
    }
    if (message.string !== undefined) {
      obj.string = message.string;
    }
    if (message.bytes !== undefined) {
      obj.bytes = base64FromBytes(message.bytes);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.duration !== undefined) {
      obj.duration = Duration.toJSON(message.duration);
    }
    if (message.nil !== undefined) {
      obj.nil = message.nil;
    }
    return obj;
  },

  create(base?: DeepPartial<Vertex>): Vertex {
    return Vertex.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Vertex>): Vertex {
    const message = createBaseVertex();
    message.key = object.key ?? "";
    message.expiration = object.expiration ?? undefined;
    message.float64 = object.float64 ?? undefined;
    message.float32 = object.float32 ?? undefined;
    message.int32 = object.int32 ?? undefined;
    message.int64 = (object.int64 !== undefined && object.int64 !== null) ? Long.fromValue(object.int64) : undefined;
    message.uint32 = object.uint32 ?? undefined;
    message.uint64 = (object.uint64 !== undefined && object.uint64 !== null)
      ? Long.fromValue(object.uint64)
      : undefined;
    message.bool = object.bool ?? undefined;
    message.string = object.string ?? undefined;
    message.bytes = object.bytes ?? undefined;
    message.timestamp = object.timestamp ?? undefined;
    message.duration = (object.duration !== undefined && object.duration !== null)
      ? Duration.fromPartial(object.duration)
      : undefined;
    message.nil = object.nil ?? undefined;
    return message;
  },
};

function createBaseEdge(): Edge {
  return { tail: "", head: "", weight: 0, expiration: undefined };
}

export const Edge: MessageFns<Edge> = {
  encode(message: Edge, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tail !== "") {
      writer.uint32(10).string(message.tail);
    }
    if (message.head !== "") {
      writer.uint32(18).string(message.head);
    }
    if (message.weight !== 0) {
      writer.uint32(29).float(message.weight);
    }
    if (message.expiration !== undefined) {
      Timestamp.encode(toTimestamp(message.expiration), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Edge {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEdge();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tail = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.head = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.weight = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expiration = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Edge {
    return {
      tail: isSet(object.tail) ? globalThis.String(object.tail) : "",
      head: isSet(object.head) ? globalThis.String(object.head) : "",
      weight: isSet(object.weight) ? globalThis.Number(object.weight) : 0,
      expiration: isSet(object.expiration) ? fromJsonTimestamp(object.expiration) : undefined,
    };
  },

  toJSON(message: Edge): unknown {
    const obj: any = {};
    if (message.tail !== "") {
      obj.tail = message.tail;
    }
    if (message.head !== "") {
      obj.head = message.head;
    }
    if (message.weight !== 0) {
      obj.weight = message.weight;
    }
    if (message.expiration !== undefined) {
      obj.expiration = message.expiration.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Edge>): Edge {
    return Edge.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Edge>): Edge {
    const message = createBaseEdge();
    message.tail = object.tail ?? "";
    message.head = object.head ?? "";
    message.weight = object.weight ?? 0;
    message.expiration = object.expiration ?? undefined;
    return message;
  },
};

function createBaseGraph(): Graph {
  return { vertices: [], edges: [] };
}

export const Graph: MessageFns<Graph> = {
  encode(message: Graph, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.vertices) {
      Vertex.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.edges) {
      Edge.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Graph {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGraph();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vertices.push(Vertex.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.edges.push(Edge.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Graph {
    return {
      vertices: globalThis.Array.isArray(object?.vertices) ? object.vertices.map((e: any) => Vertex.fromJSON(e)) : [],
      edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => Edge.fromJSON(e)) : [],
    };
  },

  toJSON(message: Graph): unknown {
    const obj: any = {};
    if (message.vertices?.length) {
      obj.vertices = message.vertices.map((e) => Vertex.toJSON(e));
    }
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => Edge.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Graph>): Graph {
    return Graph.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Graph>): Graph {
    const message = createBaseGraph();
    message.vertices = object.vertices?.map((e) => Vertex.fromPartial(e)) || [];
    message.edges = object.edges?.map((e) => Edge.fromPartial(e)) || [];
    return message;
  },
};

function createBaseIlluminateRequest(): IlluminateRequest {
  return { seed: "", step: 0, k: 0, tfidf: false, optimization: 0 };
}

export const IlluminateRequest: MessageFns<IlluminateRequest> = {
  encode(message: IlluminateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seed !== "") {
      writer.uint32(10).string(message.seed);
    }
    if (message.step !== 0) {
      writer.uint32(16).uint32(message.step);
    }
    if (message.k !== 0) {
      writer.uint32(24).uint32(message.k);
    }
    if (message.tfidf !== false) {
      writer.uint32(32).bool(message.tfidf);
    }
    if (message.optimization !== 0) {
      writer.uint32(40).int32(message.optimization);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IlluminateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIlluminateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.seed = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.step = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.k = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.tfidf = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.optimization = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IlluminateRequest {
    return {
      seed: isSet(object.seed) ? globalThis.String(object.seed) : "",
      step: isSet(object.step) ? globalThis.Number(object.step) : 0,
      k: isSet(object.k) ? globalThis.Number(object.k) : 0,
      tfidf: isSet(object.tfidf) ? globalThis.Boolean(object.tfidf) : false,
      optimization: isSet(object.optimization) ? optimizationFromJSON(object.optimization) : 0,
    };
  },

  toJSON(message: IlluminateRequest): unknown {
    const obj: any = {};
    if (message.seed !== "") {
      obj.seed = message.seed;
    }
    if (message.step !== 0) {
      obj.step = Math.round(message.step);
    }
    if (message.k !== 0) {
      obj.k = Math.round(message.k);
    }
    if (message.tfidf !== false) {
      obj.tfidf = message.tfidf;
    }
    if (message.optimization !== 0) {
      obj.optimization = optimizationToJSON(message.optimization);
    }
    return obj;
  },

  create(base?: DeepPartial<IlluminateRequest>): IlluminateRequest {
    return IlluminateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IlluminateRequest>): IlluminateRequest {
    const message = createBaseIlluminateRequest();
    message.seed = object.seed ?? "";
    message.step = object.step ?? 0;
    message.k = object.k ?? 0;
    message.tfidf = object.tfidf ?? false;
    message.optimization = object.optimization ?? 0;
    return message;
  },
};

function createBaseIlluminateResponse(): IlluminateResponse {
  return { graph: undefined };
}

export const IlluminateResponse: MessageFns<IlluminateResponse> = {
  encode(message: IlluminateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.graph !== undefined) {
      Graph.encode(message.graph, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IlluminateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIlluminateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.graph = Graph.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IlluminateResponse {
    return { graph: isSet(object.graph) ? Graph.fromJSON(object.graph) : undefined };
  },

  toJSON(message: IlluminateResponse): unknown {
    const obj: any = {};
    if (message.graph !== undefined) {
      obj.graph = Graph.toJSON(message.graph);
    }
    return obj;
  },

  create(base?: DeepPartial<IlluminateResponse>): IlluminateResponse {
    return IlluminateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IlluminateResponse>): IlluminateResponse {
    const message = createBaseIlluminateResponse();
    message.graph = (object.graph !== undefined && object.graph !== null) ? Graph.fromPartial(object.graph) : undefined;
    return message;
  },
};

function createBaseGetVertexRequest(): GetVertexRequest {
  return { key: "" };
}

export const GetVertexRequest: MessageFns<GetVertexRequest> = {
  encode(message: GetVertexRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetVertexRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetVertexRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetVertexRequest {
    return { key: isSet(object.key) ? globalThis.String(object.key) : "" };
  },

  toJSON(message: GetVertexRequest): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create(base?: DeepPartial<GetVertexRequest>): GetVertexRequest {
    return GetVertexRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetVertexRequest>): GetVertexRequest {
    const message = createBaseGetVertexRequest();
    message.key = object.key ?? "";
    return message;
  },
};

function createBaseGetVertexResponse(): GetVertexResponse {
  return { vertex: undefined };
}

export const GetVertexResponse: MessageFns<GetVertexResponse> = {
  encode(message: GetVertexResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vertex !== undefined) {
      Vertex.encode(message.vertex, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetVertexResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetVertexResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vertex = Vertex.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetVertexResponse {
    return { vertex: isSet(object.vertex) ? Vertex.fromJSON(object.vertex) : undefined };
  },

  toJSON(message: GetVertexResponse): unknown {
    const obj: any = {};
    if (message.vertex !== undefined) {
      obj.vertex = Vertex.toJSON(message.vertex);
    }
    return obj;
  },

  create(base?: DeepPartial<GetVertexResponse>): GetVertexResponse {
    return GetVertexResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetVertexResponse>): GetVertexResponse {
    const message = createBaseGetVertexResponse();
    message.vertex = (object.vertex !== undefined && object.vertex !== null)
      ? Vertex.fromPartial(object.vertex)
      : undefined;
    return message;
  },
};

function createBaseGetVerticesRequest(): GetVerticesRequest {
  return { keys: [] };
}

export const GetVerticesRequest: MessageFns<GetVerticesRequest> = {
  encode(message: GetVerticesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.keys) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetVerticesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetVerticesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keys.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetVerticesRequest {
    return { keys: globalThis.Array.isArray(object?.keys) ? object.keys.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: GetVerticesRequest): unknown {
    const obj: any = {};
    if (message.keys?.length) {
      obj.keys = message.keys;
    }
    return obj;
  },

  create(base?: DeepPartial<GetVerticesRequest>): GetVerticesRequest {
    return GetVerticesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetVerticesRequest>): GetVerticesRequest {
    const message = createBaseGetVerticesRequest();
    message.keys = object.keys?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetVerticesResponse(): GetVerticesResponse {
  return { vertices: [], missing: [] };
}

export const GetVerticesResponse: MessageFns<GetVerticesResponse> = {
  encode(message: GetVerticesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.vertices) {
      Vertex.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.missing) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetVerticesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetVerticesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vertices.push(Vertex.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.missing.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetVerticesResponse {
    return {
      vertices: globalThis.Array.isArray(object?.vertices) ? object.vertices.map((e: any) => Vertex.fromJSON(e)) : [],
      missing: globalThis.Array.isArray(object?.missing) ? object.missing.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GetVerticesResponse): unknown {
    const obj: any = {};
    if (message.vertices?.length) {
      obj.vertices = message.vertices.map((e) => Vertex.toJSON(e));
    }
    if (message.missing?.length) {
      obj.missing = message.missing;
    }
    return obj;
  },

  create(base?: DeepPartial<GetVerticesResponse>): GetVerticesResponse {
    return GetVerticesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetVerticesResponse>): GetVerticesResponse {
    const message = createBaseGetVerticesResponse();
    message.vertices = object.vertices?.map((e) => Vertex.fromPartial(e)) || [];
    message.missing = object.missing?.map((e) => e) || [];
    return message;
  },
};

function createBasePutVertexRequest(): PutVertexRequest {
  return { vertex: undefined };
}

export const PutVertexRequest: MessageFns<PutVertexRequest> = {
  encode(message: PutVertexRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vertex !== undefined) {
      Vertex.encode(message.vertex, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutVertexRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutVertexRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vertex = Vertex.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutVertexRequest {
    return { vertex: isSet(object.vertex) ? Vertex.fromJSON(object.vertex) : undefined };
  },

  toJSON(message: PutVertexRequest): unknown {
    const obj: any = {};
    if (message.vertex !== undefined) {
      obj.vertex = Vertex.toJSON(message.vertex);
    }
    return obj;
  },

  create(base?: DeepPartial<PutVertexRequest>): PutVertexRequest {
    return PutVertexRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutVertexRequest>): PutVertexRequest {
    const message = createBasePutVertexRequest();
    message.vertex = (object.vertex !== undefined && object.vertex !== null)
      ? Vertex.fromPartial(object.vertex)
      : undefined;
    return message;
  },
};

function createBasePutVertexResponse(): PutVertexResponse {
  return {};
}

export const PutVertexResponse: MessageFns<PutVertexResponse> = {
  encode(_: PutVertexResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutVertexResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutVertexResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PutVertexResponse {
    return {};
  },

  toJSON(_: PutVertexResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PutVertexResponse>): PutVertexResponse {
    return PutVertexResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PutVertexResponse>): PutVertexResponse {
    const message = createBasePutVertexResponse();
    return message;
  },
};

function createBasePutVerticesRequest(): PutVerticesRequest {
  return { vertices: [] };
}

export const PutVerticesRequest: MessageFns<PutVerticesRequest> = {
  encode(message: PutVerticesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.vertices) {
      Vertex.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutVerticesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutVerticesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vertices.push(Vertex.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutVerticesRequest {
    return {
      vertices: globalThis.Array.isArray(object?.vertices) ? object.vertices.map((e: any) => Vertex.fromJSON(e)) : [],
    };
  },

  toJSON(message: PutVerticesRequest): unknown {
    const obj: any = {};
    if (message.vertices?.length) {
      obj.vertices = message.vertices.map((e) => Vertex.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PutVerticesRequest>): PutVerticesRequest {
    return PutVerticesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutVerticesRequest>): PutVerticesRequest {
    const message = createBasePutVerticesRequest();
    message.vertices = object.vertices?.map((e) => Vertex.fromPartial(e)) || [];
    return message;
  },
};

function createBasePutVerticesResponse(): PutVerticesResponse {
  return { written: 0 };
}

export const PutVerticesResponse: MessageFns<PutVerticesResponse> = {
  encode(message: PutVerticesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.written !== 0) {
      writer.uint32(8).int32(message.written);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutVerticesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutVerticesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.written = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutVerticesResponse {
    return { written: isSet(object.written) ? globalThis.Number(object.written) : 0 };
  },

  toJSON(message: PutVerticesResponse): unknown {
    const obj: any = {};
    if (message.written !== 0) {
      obj.written = Math.round(message.written);
    }
    return obj;
  },

  create(base?: DeepPartial<PutVerticesResponse>): PutVerticesResponse {
    return PutVerticesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutVerticesResponse>): PutVerticesResponse {
    const message = createBasePutVerticesResponse();
    message.written = object.written ?? 0;
    return message;
  },
};

function createBaseDeleteVertexRequest(): DeleteVertexRequest {
  return { key: "" };
}

export const DeleteVertexRequest: MessageFns<DeleteVertexRequest> = {
  encode(message: DeleteVertexRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteVertexRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteVertexRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteVertexRequest {
    return { key: isSet(object.key) ? globalThis.String(object.key) : "" };
  },

  toJSON(message: DeleteVertexRequest): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteVertexRequest>): DeleteVertexRequest {
    return DeleteVertexRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteVertexRequest>): DeleteVertexRequest {
    const message = createBaseDeleteVertexRequest();
    message.key = object.key ?? "";
    return message;
  },
};

function createBaseDeleteVertexResponse(): DeleteVertexResponse {
  return { existed: false };
}

export const DeleteVertexResponse: MessageFns<DeleteVertexResponse> = {
  encode(message: DeleteVertexResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.existed !== false) {
      writer.uint32(8).bool(message.existed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteVertexResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteVertexResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.existed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteVertexResponse {
    return { existed: isSet(object.existed) ? globalThis.Boolean(object.existed) : false };
  },

  toJSON(message: DeleteVertexResponse): unknown {
    const obj: any = {};
    if (message.existed !== false) {
      obj.existed = message.existed;
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteVertexResponse>): DeleteVertexResponse {
    return DeleteVertexResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteVertexResponse>): DeleteVertexResponse {
    const message = createBaseDeleteVertexResponse();
    message.existed = object.existed ?? false;
    return message;
  },
};

function createBaseDeleteVerticesRequest(): DeleteVerticesRequest {
  return { keys: [] };
}

export const DeleteVerticesRequest: MessageFns<DeleteVerticesRequest> = {
  encode(message: DeleteVerticesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.keys) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteVerticesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteVerticesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keys.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteVerticesRequest {
    return { keys: globalThis.Array.isArray(object?.keys) ? object.keys.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: DeleteVerticesRequest): unknown {
    const obj: any = {};
    if (message.keys?.length) {
      obj.keys = message.keys;
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteVerticesRequest>): DeleteVerticesRequest {
    return DeleteVerticesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteVerticesRequest>): DeleteVerticesRequest {
    const message = createBaseDeleteVerticesRequest();
    message.keys = object.keys?.map((e) => e) || [];
    return message;
  },
};

function createBaseDeleteVerticesResponse(): DeleteVerticesResponse {
  return { deleted: 0 };
}

export const DeleteVerticesResponse: MessageFns<DeleteVerticesResponse> = {
  encode(message: DeleteVerticesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deleted !== 0) {
      writer.uint32(8).int32(message.deleted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteVerticesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteVerticesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deleted = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteVerticesResponse {
    return { deleted: isSet(object.deleted) ? globalThis.Number(object.deleted) : 0 };
  },

  toJSON(message: DeleteVerticesResponse): unknown {
    const obj: any = {};
    if (message.deleted !== 0) {
      obj.deleted = Math.round(message.deleted);
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteVerticesResponse>): DeleteVerticesResponse {
    return DeleteVerticesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteVerticesResponse>): DeleteVerticesResponse {
    const message = createBaseDeleteVerticesResponse();
    message.deleted = object.deleted ?? 0;
    return message;
  },
};

function createBaseScanVerticesRequest(): ScanVerticesRequest {
  return { prefix: "", limit: 0, cursor: Buffer.alloc(0) };
}

export const ScanVerticesRequest: MessageFns<ScanVerticesRequest> = {
  encode(message: ScanVerticesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== "") {
      writer.uint32(10).string(message.prefix);
    }
    if (message.limit !== 0) {
      writer.uint32(16).uint32(message.limit);
    }
    if (message.cursor.length !== 0) {
      writer.uint32(26).bytes(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScanVerticesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScanVerticesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cursor = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScanVerticesRequest {
    return {
      prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      cursor: isSet(object.cursor) ? Buffer.from(bytesFromBase64(object.cursor)) : Buffer.alloc(0),
    };
  },

  toJSON(message: ScanVerticesRequest): unknown {
    const obj: any = {};
    if (message.prefix !== "") {
      obj.prefix = message.prefix;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.cursor.length !== 0) {
      obj.cursor = base64FromBytes(message.cursor);
    }
    return obj;
  },

  create(base?: DeepPartial<ScanVerticesRequest>): ScanVerticesRequest {
    return ScanVerticesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScanVerticesRequest>): ScanVerticesRequest {
    const message = createBaseScanVerticesRequest();
    message.prefix = object.prefix ?? "";
    message.limit = object.limit ?? 0;
    message.cursor = object.cursor ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseScanVerticesResponse(): ScanVerticesResponse {
  return { vertices: [], nextCursor: Buffer.alloc(0) };
}

export const ScanVerticesResponse: MessageFns<ScanVerticesResponse> = {
  encode(message: ScanVerticesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.vertices) {
      Vertex.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor.length !== 0) {
      writer.uint32(18).bytes(message.nextCursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScanVerticesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScanVerticesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vertices.push(Vertex.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextCursor = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScanVerticesResponse {
    return {
      vertices: globalThis.Array.isArray(object?.vertices) ? object.vertices.map((e: any) => Vertex.fromJSON(e)) : [],
      nextCursor: isSet(object.nextCursor)
        ? Buffer.from(bytesFromBase64(object.nextCursor))
        : isSet(object.next_cursor)
        ? Buffer.from(bytesFromBase64(object.next_cursor))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: ScanVerticesResponse): unknown {
    const obj: any = {};
    if (message.vertices?.length) {
      obj.vertices = message.vertices.map((e) => Vertex.toJSON(e));
    }
    if (message.nextCursor.length !== 0) {
      obj.nextCursor = base64FromBytes(message.nextCursor);
    }
    return obj;
  },

  create(base?: DeepPartial<ScanVerticesResponse>): ScanVerticesResponse {
    return ScanVerticesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScanVerticesResponse>): ScanVerticesResponse {
    const message = createBaseScanVerticesResponse();
    message.vertices = object.vertices?.map((e) => Vertex.fromPartial(e)) || [];
    message.nextCursor = object.nextCursor ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseCountVerticesByPrefixRequest(): CountVerticesByPrefixRequest {
  return { prefix: "" };
}

export const CountVerticesByPrefixRequest: MessageFns<CountVerticesByPrefixRequest> = {
  encode(message: CountVerticesByPrefixRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== "") {
      writer.uint32(10).string(message.prefix);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CountVerticesByPrefixRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCountVerticesByPrefixRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CountVerticesByPrefixRequest {
    return { prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : "" };
  },

  toJSON(message: CountVerticesByPrefixRequest): unknown {
    const obj: any = {};
    if (message.prefix !== "") {
      obj.prefix = message.prefix;
    }
    return obj;
  },

  create(base?: DeepPartial<CountVerticesByPrefixRequest>): CountVerticesByPrefixRequest {
    return CountVerticesByPrefixRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CountVerticesByPrefixRequest>): CountVerticesByPrefixRequest {
    const message = createBaseCountVerticesByPrefixRequest();
    message.prefix = object.prefix ?? "";
    return message;
  },
};

function createBaseCountVerticesByPrefixResponse(): CountVerticesByPrefixResponse {
  return { count: Long.UZERO };
}

export const CountVerticesByPrefixResponse: MessageFns<CountVerticesByPrefixResponse> = {
  encode(message: CountVerticesByPrefixResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.count.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.count.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CountVerticesByPrefixResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCountVerticesByPrefixResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.count = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CountVerticesByPrefixResponse {
    return { count: isSet(object.count) ? Long.fromValue(object.count) : Long.UZERO };
  },

  toJSON(message: CountVerticesByPrefixResponse): unknown {
    const obj: any = {};
    if (!message.count.equals(Long.UZERO)) {
      obj.count = (message.count || Long.UZERO).toString();
    }
    return obj;
  },

  create(base?: DeepPartial<CountVerticesByPrefixResponse>): CountVerticesByPrefixResponse {
    return CountVerticesByPrefixResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CountVerticesByPrefixResponse>): CountVerticesByPrefixResponse {
    const message = createBaseCountVerticesByPrefixResponse();
    message.count = (object.count !== undefined && object.count !== null) ? Long.fromValue(object.count) : Long.UZERO;
    return message;
  },
};

function createBaseDeleteVerticesByPrefixRequest(): DeleteVerticesByPrefixRequest {
  return { prefix: "", limit: 0, dryRun: false };
}

export const DeleteVerticesByPrefixRequest: MessageFns<DeleteVerticesByPrefixRequest> = {
  encode(message: DeleteVerticesByPrefixRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== "") {
      writer.uint32(10).string(message.prefix);
    }
    if (message.limit !== 0) {
      writer.uint32(16).uint32(message.limit);
    }
    if (message.dryRun !== false) {
      writer.uint32(24).bool(message.dryRun);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteVerticesByPrefixRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteVerticesByPrefixRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dryRun = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteVerticesByPrefixRequest {
    return {
      prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      dryRun: isSet(object.dryRun)
        ? globalThis.Boolean(object.dryRun)
        : isSet(object.dry_run)
        ? globalThis.Boolean(object.dry_run)
        : false,
    };
  },

  toJSON(message: DeleteVerticesByPrefixRequest): unknown {
    const obj: any = {};
    if (message.prefix !== "") {
      obj.prefix = message.prefix;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.dryRun !== false) {
      obj.dryRun = message.dryRun;
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteVerticesByPrefixRequest>): DeleteVerticesByPrefixRequest {
    return DeleteVerticesByPrefixRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteVerticesByPrefixRequest>): DeleteVerticesByPrefixRequest {
    const message = createBaseDeleteVerticesByPrefixRequest();
    message.prefix = object.prefix ?? "";
    message.limit = object.limit ?? 0;
    message.dryRun = object.dryRun ?? false;
    return message;
  },
};

function createBaseDeleteVerticesByPrefixResponse(): DeleteVerticesByPrefixResponse {
  return { deleted: Long.UZERO };
}

export const DeleteVerticesByPrefixResponse: MessageFns<DeleteVerticesByPrefixResponse> = {
  encode(message: DeleteVerticesByPrefixResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.deleted.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.deleted.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteVerticesByPrefixResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteVerticesByPrefixResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deleted = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteVerticesByPrefixResponse {
    return { deleted: isSet(object.deleted) ? Long.fromValue(object.deleted) : Long.UZERO };
  },

  toJSON(message: DeleteVerticesByPrefixResponse): unknown {
    const obj: any = {};
    if (!message.deleted.equals(Long.UZERO)) {
      obj.deleted = (message.deleted || Long.UZERO).toString();
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteVerticesByPrefixResponse>): DeleteVerticesByPrefixResponse {
    return DeleteVerticesByPrefixResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteVerticesByPrefixResponse>): DeleteVerticesByPrefixResponse {
    const message = createBaseDeleteVerticesByPrefixResponse();
    message.deleted = (object.deleted !== undefined && object.deleted !== null)
      ? Long.fromValue(object.deleted)
      : Long.UZERO;
    return message;
  },
};

function createBaseGetEdgeRequest(): GetEdgeRequest {
  return { tail: "", head: "" };
}

export const GetEdgeRequest: MessageFns<GetEdgeRequest> = {
  encode(message: GetEdgeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tail !== "") {
      writer.uint32(10).string(message.tail);
    }
    if (message.head !== "") {
      writer.uint32(18).string(message.head);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEdgeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEdgeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tail = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.head = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEdgeRequest {
    return {
      tail: isSet(object.tail) ? globalThis.String(object.tail) : "",
      head: isSet(object.head) ? globalThis.String(object.head) : "",
    };
  },

  toJSON(message: GetEdgeRequest): unknown {
    const obj: any = {};
    if (message.tail !== "") {
      obj.tail = message.tail;
    }
    if (message.head !== "") {
      obj.head = message.head;
    }
    return obj;
  },

  create(base?: DeepPartial<GetEdgeRequest>): GetEdgeRequest {
    return GetEdgeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEdgeRequest>): GetEdgeRequest {
    const message = createBaseGetEdgeRequest();
    message.tail = object.tail ?? "";
    message.head = object.head ?? "";
    return message;
  },
};

function createBaseGetEdgeResponse(): GetEdgeResponse {
  return { edge: undefined };
}

export const GetEdgeResponse: MessageFns<GetEdgeResponse> = {
  encode(message: GetEdgeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.edge !== undefined) {
      Edge.encode(message.edge, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEdgeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEdgeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edge = Edge.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEdgeResponse {
    return { edge: isSet(object.edge) ? Edge.fromJSON(object.edge) : undefined };
  },

  toJSON(message: GetEdgeResponse): unknown {
    const obj: any = {};
    if (message.edge !== undefined) {
      obj.edge = Edge.toJSON(message.edge);
    }
    return obj;
  },

  create(base?: DeepPartial<GetEdgeResponse>): GetEdgeResponse {
    return GetEdgeResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEdgeResponse>): GetEdgeResponse {
    const message = createBaseGetEdgeResponse();
    message.edge = (object.edge !== undefined && object.edge !== null) ? Edge.fromPartial(object.edge) : undefined;
    return message;
  },
};

function createBaseGetEdgesRequest(): GetEdgesRequest {
  return { edges: [] };
}

export const GetEdgesRequest: MessageFns<GetEdgesRequest> = {
  encode(message: GetEdgesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.edges) {
      EdgeKey.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEdgesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEdgesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edges.push(EdgeKey.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEdgesRequest {
    return { edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => EdgeKey.fromJSON(e)) : [] };
  },

  toJSON(message: GetEdgesRequest): unknown {
    const obj: any = {};
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => EdgeKey.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetEdgesRequest>): GetEdgesRequest {
    return GetEdgesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEdgesRequest>): GetEdgesRequest {
    const message = createBaseGetEdgesRequest();
    message.edges = object.edges?.map((e) => EdgeKey.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetEdgesResponse(): GetEdgesResponse {
  return { edges: [], missing: [] };
}

export const GetEdgesResponse: MessageFns<GetEdgesResponse> = {
  encode(message: GetEdgesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.edges) {
      Edge.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.missing) {
      EdgeKey.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEdgesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEdgesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edges.push(Edge.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.missing.push(EdgeKey.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEdgesResponse {
    return {
      edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => Edge.fromJSON(e)) : [],
      missing: globalThis.Array.isArray(object?.missing) ? object.missing.map((e: any) => EdgeKey.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetEdgesResponse): unknown {
    const obj: any = {};
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => Edge.toJSON(e));
    }
    if (message.missing?.length) {
      obj.missing = message.missing.map((e) => EdgeKey.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetEdgesResponse>): GetEdgesResponse {
    return GetEdgesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEdgesResponse>): GetEdgesResponse {
    const message = createBaseGetEdgesResponse();
    message.edges = object.edges?.map((e) => Edge.fromPartial(e)) || [];
    message.missing = object.missing?.map((e) => EdgeKey.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeleteEdgeRequest(): DeleteEdgeRequest {
  return { tail: "", head: "" };
}

export const DeleteEdgeRequest: MessageFns<DeleteEdgeRequest> = {
  encode(message: DeleteEdgeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tail !== "") {
      writer.uint32(10).string(message.tail);
    }
    if (message.head !== "") {
      writer.uint32(18).string(message.head);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteEdgeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteEdgeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tail = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.head = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteEdgeRequest {
    return {
      tail: isSet(object.tail) ? globalThis.String(object.tail) : "",
      head: isSet(object.head) ? globalThis.String(object.head) : "",
    };
  },

  toJSON(message: DeleteEdgeRequest): unknown {
    const obj: any = {};
    if (message.tail !== "") {
      obj.tail = message.tail;
    }
    if (message.head !== "") {
      obj.head = message.head;
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteEdgeRequest>): DeleteEdgeRequest {
    return DeleteEdgeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteEdgeRequest>): DeleteEdgeRequest {
    const message = createBaseDeleteEdgeRequest();
    message.tail = object.tail ?? "";
    message.head = object.head ?? "";
    return message;
  },
};

function createBaseDeleteEdgeResponse(): DeleteEdgeResponse {
  return { existed: false };
}

export const DeleteEdgeResponse: MessageFns<DeleteEdgeResponse> = {
  encode(message: DeleteEdgeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.existed !== false) {
      writer.uint32(8).bool(message.existed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteEdgeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteEdgeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.existed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteEdgeResponse {
    return { existed: isSet(object.existed) ? globalThis.Boolean(object.existed) : false };
  },

  toJSON(message: DeleteEdgeResponse): unknown {
    const obj: any = {};
    if (message.existed !== false) {
      obj.existed = message.existed;
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteEdgeResponse>): DeleteEdgeResponse {
    return DeleteEdgeResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteEdgeResponse>): DeleteEdgeResponse {
    const message = createBaseDeleteEdgeResponse();
    message.existed = object.existed ?? false;
    return message;
  },
};

function createBaseEdgeKey(): EdgeKey {
  return { tail: "", head: "" };
}

export const EdgeKey: MessageFns<EdgeKey> = {
  encode(message: EdgeKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tail !== "") {
      writer.uint32(10).string(message.tail);
    }
    if (message.head !== "") {
      writer.uint32(18).string(message.head);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EdgeKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEdgeKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tail = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.head = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EdgeKey {
    return {
      tail: isSet(object.tail) ? globalThis.String(object.tail) : "",
      head: isSet(object.head) ? globalThis.String(object.head) : "",
    };
  },

  toJSON(message: EdgeKey): unknown {
    const obj: any = {};
    if (message.tail !== "") {
      obj.tail = message.tail;
    }
    if (message.head !== "") {
      obj.head = message.head;
    }
    return obj;
  },

  create(base?: DeepPartial<EdgeKey>): EdgeKey {
    return EdgeKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EdgeKey>): EdgeKey {
    const message = createBaseEdgeKey();
    message.tail = object.tail ?? "";
    message.head = object.head ?? "";
    return message;
  },
};

function createBaseScanEdgesRequest(): ScanEdgesRequest {
  return { tailPrefix: "", headPrefix: "", limit: 0, cursor: Buffer.alloc(0) };
}

export const ScanEdgesRequest: MessageFns<ScanEdgesRequest> = {
  encode(message: ScanEdgesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tailPrefix !== "") {
      writer.uint32(10).string(message.tailPrefix);
    }
    if (message.headPrefix !== "") {
      writer.uint32(18).string(message.headPrefix);
    }
    if (message.limit !== 0) {
      writer.uint32(24).uint32(message.limit);
    }
    if (message.cursor.length !== 0) {
      writer.uint32(34).bytes(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScanEdgesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScanEdgesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tailPrefix = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.headPrefix = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cursor = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScanEdgesRequest {
    return {
      tailPrefix: isSet(object.tailPrefix)
        ? globalThis.String(object.tailPrefix)
        : isSet(object.tail_prefix)
        ? globalThis.String(object.tail_prefix)
        : "",
      headPrefix: isSet(object.headPrefix)
        ? globalThis.String(object.headPrefix)
        : isSet(object.head_prefix)
        ? globalThis.String(object.head_prefix)
        : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      cursor: isSet(object.cursor) ? Buffer.from(bytesFromBase64(object.cursor)) : Buffer.alloc(0),
    };
  },

  toJSON(message: ScanEdgesRequest): unknown {
    const obj: any = {};
    if (message.tailPrefix !== "") {
      obj.tailPrefix = message.tailPrefix;
    }
    if (message.headPrefix !== "") {
      obj.headPrefix = message.headPrefix;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.cursor.length !== 0) {
      obj.cursor = base64FromBytes(message.cursor);
    }
    return obj;
  },

  create(base?: DeepPartial<ScanEdgesRequest>): ScanEdgesRequest {
    return ScanEdgesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScanEdgesRequest>): ScanEdgesRequest {
    const message = createBaseScanEdgesRequest();
    message.tailPrefix = object.tailPrefix ?? "";
    message.headPrefix = object.headPrefix ?? "";
    message.limit = object.limit ?? 0;
    message.cursor = object.cursor ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseScanEdgesResponse(): ScanEdgesResponse {
  return { edges: [], nextCursor: Buffer.alloc(0) };
}

export const ScanEdgesResponse: MessageFns<ScanEdgesResponse> = {
  encode(message: ScanEdgesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.edges) {
      Edge.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor.length !== 0) {
      writer.uint32(18).bytes(message.nextCursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScanEdgesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScanEdgesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edges.push(Edge.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextCursor = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScanEdgesResponse {
    return {
      edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => Edge.fromJSON(e)) : [],
      nextCursor: isSet(object.nextCursor)
        ? Buffer.from(bytesFromBase64(object.nextCursor))
        : isSet(object.next_cursor)
        ? Buffer.from(bytesFromBase64(object.next_cursor))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: ScanEdgesResponse): unknown {
    const obj: any = {};
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => Edge.toJSON(e));
    }
    if (message.nextCursor.length !== 0) {
      obj.nextCursor = base64FromBytes(message.nextCursor);
    }
    return obj;
  },

  create(base?: DeepPartial<ScanEdgesResponse>): ScanEdgesResponse {
    return ScanEdgesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScanEdgesResponse>): ScanEdgesResponse {
    const message = createBaseScanEdgesResponse();
    message.edges = object.edges?.map((e) => Edge.fromPartial(e)) || [];
    message.nextCursor = object.nextCursor ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseDeleteEdgesRequest(): DeleteEdgesRequest {
  return { edges: [] };
}

export const DeleteEdgesRequest: MessageFns<DeleteEdgesRequest> = {
  encode(message: DeleteEdgesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.edges) {
      EdgeKey.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteEdgesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteEdgesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edges.push(EdgeKey.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteEdgesRequest {
    return { edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => EdgeKey.fromJSON(e)) : [] };
  },

  toJSON(message: DeleteEdgesRequest): unknown {
    const obj: any = {};
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => EdgeKey.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteEdgesRequest>): DeleteEdgesRequest {
    return DeleteEdgesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteEdgesRequest>): DeleteEdgesRequest {
    const message = createBaseDeleteEdgesRequest();
    message.edges = object.edges?.map((e) => EdgeKey.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeleteEdgesResponse(): DeleteEdgesResponse {
  return { deleted: 0 };
}

export const DeleteEdgesResponse: MessageFns<DeleteEdgesResponse> = {
  encode(message: DeleteEdgesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deleted !== 0) {
      writer.uint32(8).int32(message.deleted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteEdgesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteEdgesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deleted = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteEdgesResponse {
    return { deleted: isSet(object.deleted) ? globalThis.Number(object.deleted) : 0 };
  },

  toJSON(message: DeleteEdgesResponse): unknown {
    const obj: any = {};
    if (message.deleted !== 0) {
      obj.deleted = Math.round(message.deleted);
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteEdgesResponse>): DeleteEdgesResponse {
    return DeleteEdgesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteEdgesResponse>): DeleteEdgesResponse {
    const message = createBaseDeleteEdgesResponse();
    message.deleted = object.deleted ?? 0;
    return message;
  },
};

function createBaseAddEdgeRequest(): AddEdgeRequest {
  return { edge: undefined };
}

export const AddEdgeRequest: MessageFns<AddEdgeRequest> = {
  encode(message: AddEdgeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.edge !== undefined) {
      Edge.encode(message.edge, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddEdgeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddEdgeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edge = Edge.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddEdgeRequest {
    return { edge: isSet(object.edge) ? Edge.fromJSON(object.edge) : undefined };
  },

  toJSON(message: AddEdgeRequest): unknown {
    const obj: any = {};
    if (message.edge !== undefined) {
      obj.edge = Edge.toJSON(message.edge);
    }
    return obj;
  },

  create(base?: DeepPartial<AddEdgeRequest>): AddEdgeRequest {
    return AddEdgeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AddEdgeRequest>): AddEdgeRequest {
    const message = createBaseAddEdgeRequest();
    message.edge = (object.edge !== undefined && object.edge !== null) ? Edge.fromPartial(object.edge) : undefined;
    return message;
  },
};

function createBaseAddEdgeResponse(): AddEdgeResponse {
  return {};
}

export const AddEdgeResponse: MessageFns<AddEdgeResponse> = {
  encode(_: AddEdgeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddEdgeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddEdgeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): AddEdgeResponse {
    return {};
  },

  toJSON(_: AddEdgeResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<AddEdgeResponse>): AddEdgeResponse {
    return AddEdgeResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<AddEdgeResponse>): AddEdgeResponse {
    const message = createBaseAddEdgeResponse();
    return message;
  },
};

function createBaseAddEdgesRequest(): AddEdgesRequest {
  return { edges: [] };
}

export const AddEdgesRequest: MessageFns<AddEdgesRequest> = {
  encode(message: AddEdgesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.edges) {
      Edge.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddEdgesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddEdgesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edges.push(Edge.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddEdgesRequest {
    return { edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => Edge.fromJSON(e)) : [] };
  },

  toJSON(message: AddEdgesRequest): unknown {
    const obj: any = {};
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => Edge.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<AddEdgesRequest>): AddEdgesRequest {
    return AddEdgesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AddEdgesRequest>): AddEdgesRequest {
    const message = createBaseAddEdgesRequest();
    message.edges = object.edges?.map((e) => Edge.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAddEdgesResponse(): AddEdgesResponse {
  return { written: 0 };
}

export const AddEdgesResponse: MessageFns<AddEdgesResponse> = {
  encode(message: AddEdgesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.written !== 0) {
      writer.uint32(8).int32(message.written);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddEdgesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddEdgesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.written = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddEdgesResponse {
    return { written: isSet(object.written) ? globalThis.Number(object.written) : 0 };
  },

  toJSON(message: AddEdgesResponse): unknown {
    const obj: any = {};
    if (message.written !== 0) {
      obj.written = Math.round(message.written);
    }
    return obj;
  },

  create(base?: DeepPartial<AddEdgesResponse>): AddEdgesResponse {
    return AddEdgesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AddEdgesResponse>): AddEdgesResponse {
    const message = createBaseAddEdgesResponse();
    message.written = object.written ?? 0;
    return message;
  },
};

function createBasePutEdgeRequest(): PutEdgeRequest {
  return { edge: undefined };
}

export const PutEdgeRequest: MessageFns<PutEdgeRequest> = {
  encode(message: PutEdgeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.edge !== undefined) {
      Edge.encode(message.edge, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutEdgeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutEdgeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edge = Edge.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutEdgeRequest {
    return { edge: isSet(object.edge) ? Edge.fromJSON(object.edge) : undefined };
  },

  toJSON(message: PutEdgeRequest): unknown {
    const obj: any = {};
    if (message.edge !== undefined) {
      obj.edge = Edge.toJSON(message.edge);
    }
    return obj;
  },

  create(base?: DeepPartial<PutEdgeRequest>): PutEdgeRequest {
    return PutEdgeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutEdgeRequest>): PutEdgeRequest {
    const message = createBasePutEdgeRequest();
    message.edge = (object.edge !== undefined && object.edge !== null) ? Edge.fromPartial(object.edge) : undefined;
    return message;
  },
};

function createBasePutEdgeResponse(): PutEdgeResponse {
  return {};
}

export const PutEdgeResponse: MessageFns<PutEdgeResponse> = {
  encode(_: PutEdgeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutEdgeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutEdgeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PutEdgeResponse {
    return {};
  },

  toJSON(_: PutEdgeResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PutEdgeResponse>): PutEdgeResponse {
    return PutEdgeResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PutEdgeResponse>): PutEdgeResponse {
    const message = createBasePutEdgeResponse();
    return message;
  },
};

function createBasePutEdgesRequest(): PutEdgesRequest {
  return { edges: [] };
}

export const PutEdgesRequest: MessageFns<PutEdgesRequest> = {
  encode(message: PutEdgesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.edges) {
      Edge.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutEdgesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutEdgesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edges.push(Edge.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutEdgesRequest {
    return { edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => Edge.fromJSON(e)) : [] };
  },

  toJSON(message: PutEdgesRequest): unknown {
    const obj: any = {};
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => Edge.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PutEdgesRequest>): PutEdgesRequest {
    return PutEdgesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutEdgesRequest>): PutEdgesRequest {
    const message = createBasePutEdgesRequest();
    message.edges = object.edges?.map((e) => Edge.fromPartial(e)) || [];
    return message;
  },
};

function createBasePutEdgesResponse(): PutEdgesResponse {
  return { written: 0 };
}

export const PutEdgesResponse: MessageFns<PutEdgesResponse> = {
  encode(message: PutEdgesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.written !== 0) {
      writer.uint32(8).int32(message.written);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutEdgesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutEdgesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.written = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutEdgesResponse {
    return { written: isSet(object.written) ? globalThis.Number(object.written) : 0 };
  },

  toJSON(message: PutEdgesResponse): unknown {
    const obj: any = {};
    if (message.written !== 0) {
      obj.written = Math.round(message.written);
    }
    return obj;
  },

  create(base?: DeepPartial<PutEdgesResponse>): PutEdgesResponse {
    return PutEdgesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutEdgesResponse>): PutEdgesResponse {
    const message = createBasePutEdgesResponse();
    message.written = object.written ?? 0;
    return message;
  },
};

function createBaseGetServerStatusRequest(): GetServerStatusRequest {
  return {};
}

export const GetServerStatusRequest: MessageFns<GetServerStatusRequest> = {
  encode(_: GetServerStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetServerStatusRequest {
    return {};
  },

  toJSON(_: GetServerStatusRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetServerStatusRequest>): GetServerStatusRequest {
    return GetServerStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetServerStatusRequest>): GetServerStatusRequest {
    const message = createBaseGetServerStatusRequest();
    return message;
  },
};

function createBaseGetServerStatusResponse(): GetServerStatusResponse {
  return {
    version: "",
    goVersion: "",
    startedAt: undefined,
    uptime: undefined,
    defaultTtl: undefined,
    maxBatchSize: 0,
    maxKeyBytes: 0,
    scanDefaultLimit: 0,
    scanMaxLimit: 0,
    tlsEnabled: false,
    replicationEnabled: false,
    vertexCount: Long.UZERO,
    edgeCount: Long.UZERO,
  };
}

export const GetServerStatusResponse: MessageFns<GetServerStatusResponse> = {
  encode(message: GetServerStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.goVersion !== "") {
      writer.uint32(18).string(message.goVersion);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(26).fork()).join();
    }
    if (message.uptime !== undefined) {
      Duration.encode(message.uptime, writer.uint32(34).fork()).join();
    }
    if (message.defaultTtl !== undefined) {
      Duration.encode(message.defaultTtl, writer.uint32(42).fork()).join();
    }
    if (message.maxBatchSize !== 0) {
      writer.uint32(48).uint32(message.maxBatchSize);
    }
    if (message.maxKeyBytes !== 0) {
      writer.uint32(56).uint32(message.maxKeyBytes);
    }
    if (message.scanDefaultLimit !== 0) {
      writer.uint32(64).uint32(message.scanDefaultLimit);
    }
    if (message.scanMaxLimit !== 0) {
      writer.uint32(72).uint32(message.scanMaxLimit);
    }
    if (message.tlsEnabled !== false) {
      writer.uint32(80).bool(message.tlsEnabled);
    }
    if (message.replicationEnabled !== false) {
      writer.uint32(88).bool(message.replicationEnabled);
    }
    if (!message.vertexCount.equals(Long.UZERO)) {
      writer.uint32(96).uint64(message.vertexCount.toString());
    }
    if (!message.edgeCount.equals(Long.UZERO)) {
      writer.uint32(104).uint64(message.edgeCount.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.goVersion = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.uptime = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.defaultTtl = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maxBatchSize = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxKeyBytes = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.scanDefaultLimit = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.scanMaxLimit = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.tlsEnabled = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.replicationEnabled = reader.bool();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.vertexCount = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.edgeCount = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetServerStatusResponse {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      goVersion: isSet(object.goVersion)
        ? globalThis.String(object.goVersion)
        : isSet(object.go_version)
        ? globalThis.String(object.go_version)
        : "",
      startedAt: isSet(object.startedAt)
        ? fromJsonTimestamp(object.startedAt)
        : isSet(object.started_at)
        ? fromJsonTimestamp(object.started_at)
        : undefined,
      uptime: isSet(object.uptime) ? Duration.fromJSON(object.uptime) : undefined,
      defaultTtl: isSet(object.defaultTtl)
        ? Duration.fromJSON(object.defaultTtl)
        : isSet(object.default_ttl)
        ? Duration.fromJSON(object.default_ttl)
        : undefined,
      maxBatchSize: isSet(object.maxBatchSize)
        ? globalThis.Number(object.maxBatchSize)
        : isSet(object.max_batch_size)
        ? globalThis.Number(object.max_batch_size)
        : 0,
      maxKeyBytes: isSet(object.maxKeyBytes)
        ? globalThis.Number(object.maxKeyBytes)
        : isSet(object.max_key_bytes)
        ? globalThis.Number(object.max_key_bytes)
        : 0,
      scanDefaultLimit: isSet(object.scanDefaultLimit)
        ? globalThis.Number(object.scanDefaultLimit)
        : isSet(object.scan_default_limit)
        ? globalThis.Number(object.scan_default_limit)
        : 0,
      scanMaxLimit: isSet(object.scanMaxLimit)
        ? globalThis.Number(object.scanMaxLimit)
        : isSet(object.scan_max_limit)
        ? globalThis.Number(object.scan_max_limit)
        : 0,
      tlsEnabled: isSet(object.tlsEnabled)
        ? globalThis.Boolean(object.tlsEnabled)
        : isSet(object.tls_enabled)
        ? globalThis.Boolean(object.tls_enabled)
        : false,
      replicationEnabled: isSet(object.replicationEnabled)
        ? globalThis.Boolean(object.replicationEnabled)
        : isSet(object.replication_enabled)
        ? globalThis.Boolean(object.replication_enabled)
        : false,
      vertexCount: isSet(object.vertexCount)
        ? Long.fromValue(object.vertexCount)
        : isSet(object.vertex_count)
        ? Long.fromValue(object.vertex_count)
        : Long.UZERO,
      edgeCount: isSet(object.edgeCount)
        ? Long.fromValue(object.edgeCount)
        : isSet(object.edge_count)
        ? Long.fromValue(object.edge_count)
        : Long.UZERO,
    };
  },

  toJSON(message: GetServerStatusResponse): unknown {
    const obj: any = {};
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.goVersion !== "") {
      obj.goVersion = message.goVersion;
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.uptime !== undefined) {
      obj.uptime = Duration.toJSON(message.uptime);
    }
    if (message.defaultTtl !== undefined) {
      obj.defaultTtl = Duration.toJSON(message.defaultTtl);
    }
    if (message.maxBatchSize !== 0) {
      obj.maxBatchSize = Math.round(message.maxBatchSize);
    }
    if (message.maxKeyBytes !== 0) {
      obj.maxKeyBytes = Math.round(message.maxKeyBytes);
    }
    if (message.scanDefaultLimit !== 0) {
      obj.scanDefaultLimit = Math.round(message.scanDefaultLimit);
    }
    if (message.scanMaxLimit !== 0) {
      obj.scanMaxLimit = Math.round(message.scanMaxLimit);
    }
    if (message.tlsEnabled !== false) {
      obj.tlsEnabled = message.tlsEnabled;
    }
    if (message.replicationEnabled !== false) {
      obj.replicationEnabled = message.replicationEnabled;
    }
    if (!message.vertexCount.equals(Long.UZERO)) {
      obj.vertexCount = (message.vertexCount || Long.UZERO).toString();
    }
    if (!message.edgeCount.equals(Long.UZERO)) {
      obj.edgeCount = (message.edgeCount || Long.UZERO).toString();
    }
    return obj;
  },

  create(base?: DeepPartial<GetServerStatusResponse>): GetServerStatusResponse {
    return GetServerStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetServerStatusResponse>): GetServerStatusResponse {
    const message = createBaseGetServerStatusResponse();
    message.version = object.version ?? "";
    message.goVersion = object.goVersion ?? "";
    message.startedAt = object.startedAt ?? undefined;
    message.uptime = (object.uptime !== undefined && object.uptime !== null)
      ? Duration.fromPartial(object.uptime)
      : undefined;
    message.defaultTtl = (object.defaultTtl !== undefined && object.defaultTtl !== null)
      ? Duration.fromPartial(object.defaultTtl)
      : undefined;
    message.maxBatchSize = object.maxBatchSize ?? 0;
    message.maxKeyBytes = object.maxKeyBytes ?? 0;
    message.scanDefaultLimit = object.scanDefaultLimit ?? 0;
    message.scanMaxLimit = object.scanMaxLimit ?? 0;
    message.tlsEnabled = object.tlsEnabled ?? false;
    message.replicationEnabled = object.replicationEnabled ?? false;
    message.vertexCount = (object.vertexCount !== undefined && object.vertexCount !== null)
      ? Long.fromValue(object.vertexCount)
      : Long.UZERO;
    message.edgeCount = (object.edgeCount !== undefined && object.edgeCount !== null)
      ? Long.fromValue(object.edgeCount)
      : Long.UZERO;
    return message;
  },
};

function createBaseReplicationPeer(): ReplicationPeer {
  return { address: "", state: 0, lastEventAt: undefined, appliedSeq: Long.UZERO, error: "" };
}

export const ReplicationPeer: MessageFns<ReplicationPeer> = {
  encode(message: ReplicationPeer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.lastEventAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastEventAt), writer.uint32(26).fork()).join();
    }
    if (!message.appliedSeq.equals(Long.UZERO)) {
      writer.uint32(32).uint64(message.appliedSeq.toString());
    }
    if (message.error !== "") {
      writer.uint32(42).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReplicationPeer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplicationPeer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lastEventAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.appliedSeq = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReplicationPeer {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      state: isSet(object.state) ? replicationPeer_StateFromJSON(object.state) : 0,
      lastEventAt: isSet(object.lastEventAt)
        ? fromJsonTimestamp(object.lastEventAt)
        : isSet(object.last_event_at)
        ? fromJsonTimestamp(object.last_event_at)
        : undefined,
      appliedSeq: isSet(object.appliedSeq)
        ? Long.fromValue(object.appliedSeq)
        : isSet(object.applied_seq)
        ? Long.fromValue(object.applied_seq)
        : Long.UZERO,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: ReplicationPeer): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.state !== 0) {
      obj.state = replicationPeer_StateToJSON(message.state);
    }
    if (message.lastEventAt !== undefined) {
      obj.lastEventAt = message.lastEventAt.toISOString();
    }
    if (!message.appliedSeq.equals(Long.UZERO)) {
      obj.appliedSeq = (message.appliedSeq || Long.UZERO).toString();
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<ReplicationPeer>): ReplicationPeer {
    return ReplicationPeer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReplicationPeer>): ReplicationPeer {
    const message = createBaseReplicationPeer();
    message.address = object.address ?? "";
    message.state = object.state ?? 0;
    message.lastEventAt = object.lastEventAt ?? undefined;
    message.appliedSeq = (object.appliedSeq !== undefined && object.appliedSeq !== null)
      ? Long.fromValue(object.appliedSeq)
      : Long.UZERO;
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseGetReplicationStatusRequest(): GetReplicationStatusRequest {
  return {};
}

export const GetReplicationStatusRequest: MessageFns<GetReplicationStatusRequest> = {
  encode(_: GetReplicationStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetReplicationStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetReplicationStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetReplicationStatusRequest {
    return {};
  },

  toJSON(_: GetReplicationStatusRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetReplicationStatusRequest>): GetReplicationStatusRequest {
    return GetReplicationStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetReplicationStatusRequest>): GetReplicationStatusRequest {
    const message = createBaseGetReplicationStatusRequest();
    return message;
  },
};

function createBaseGetReplicationStatusResponse(): GetReplicationStatusResponse {
  return { nodeId: "", localNow: undefined, enabled: false, peers: [] };
}

export const GetReplicationStatusResponse: MessageFns<GetReplicationStatusResponse> = {
  encode(message: GetReplicationStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.localNow !== undefined) {
      Timestamp.encode(toTimestamp(message.localNow), writer.uint32(18).fork()).join();
    }
    if (message.enabled !== false) {
      writer.uint32(24).bool(message.enabled);
    }
    for (const v of message.peers) {
      ReplicationPeer.encode(v!, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetReplicationStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetReplicationStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.localNow = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.peers.push(ReplicationPeer.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetReplicationStatusResponse {
    return {
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      localNow: isSet(object.localNow)
        ? fromJsonTimestamp(object.localNow)
        : isSet(object.local_now)
        ? fromJsonTimestamp(object.local_now)
        : undefined,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      peers: globalThis.Array.isArray(object?.peers) ? object.peers.map((e: any) => ReplicationPeer.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetReplicationStatusResponse): unknown {
    const obj: any = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.localNow !== undefined) {
      obj.localNow = message.localNow.toISOString();
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.peers?.length) {
      obj.peers = message.peers.map((e) => ReplicationPeer.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetReplicationStatusResponse>): GetReplicationStatusResponse {
    return GetReplicationStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetReplicationStatusResponse>): GetReplicationStatusResponse {
    const message = createBaseGetReplicationStatusResponse();
    message.nodeId = object.nodeId ?? "";
    message.localNow = object.localNow ?? undefined;
    message.enabled = object.enabled ?? false;
    message.peers = object.peers?.map((e) => ReplicationPeer.fromPartial(e)) || [];
    return message;
  },
};

export type LanternServiceService = typeof LanternServiceService;
export const LanternServiceService = {
  illuminate: {
    path: "/graph.v1.LanternService/Illuminate" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: IlluminateRequest): Buffer => Buffer.from(IlluminateRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): IlluminateRequest => IlluminateRequest.decode(value),
    responseSerialize: (value: IlluminateResponse): Buffer => Buffer.from(IlluminateResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): IlluminateResponse => IlluminateResponse.decode(value),
  },
  getVertex: {
    path: "/graph.v1.LanternService/GetVertex" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetVertexRequest): Buffer => Buffer.from(GetVertexRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetVertexRequest => GetVertexRequest.decode(value),
    responseSerialize: (value: GetVertexResponse): Buffer => Buffer.from(GetVertexResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetVertexResponse => GetVertexResponse.decode(value),
  },
  /** GetVertices reads several vertices in one round trip. */
  getVertices: {
    path: "/graph.v1.LanternService/GetVertices" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetVerticesRequest): Buffer => Buffer.from(GetVerticesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetVerticesRequest => GetVerticesRequest.decode(value),
    responseSerialize: (value: GetVerticesResponse): Buffer => Buffer.from(GetVerticesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetVerticesResponse => GetVerticesResponse.decode(value),
  },
  /** PutVertex writes a single vertex. Thin facade over PutVertices. */
  putVertex: {
    path: "/graph.v1.LanternService/PutVertex" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PutVertexRequest): Buffer => Buffer.from(PutVertexRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PutVertexRequest => PutVertexRequest.decode(value),
    responseSerialize: (value: PutVertexResponse): Buffer => Buffer.from(PutVertexResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PutVertexResponse => PutVertexResponse.decode(value),
  },
  putVertices: {
    path: "/graph.v1.LanternService/PutVertices" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PutVerticesRequest): Buffer => Buffer.from(PutVerticesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PutVerticesRequest => PutVerticesRequest.decode(value),
    responseSerialize: (value: PutVerticesResponse): Buffer => Buffer.from(PutVerticesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PutVerticesResponse => PutVerticesResponse.decode(value),
  },
  deleteVertex: {
    path: "/graph.v1.LanternService/DeleteVertex" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteVertexRequest): Buffer => Buffer.from(DeleteVertexRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteVertexRequest => DeleteVertexRequest.decode(value),
    responseSerialize: (value: DeleteVertexResponse): Buffer =>
      Buffer.from(DeleteVertexResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteVertexResponse => DeleteVertexResponse.decode(value),
  },
  /** DeleteVertices removes several vertices in one round trip. */
  deleteVertices: {
    path: "/graph.v1.LanternService/DeleteVertices" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteVerticesRequest): Buffer =>
      Buffer.from(DeleteVerticesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteVerticesRequest => DeleteVerticesRequest.decode(value),
    responseSerialize: (value: DeleteVerticesResponse): Buffer =>
      Buffer.from(DeleteVerticesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteVerticesResponse => DeleteVerticesResponse.decode(value),
  },
  /**
   * ScanVertices streams vertices whose key starts with the given prefix in
   * ascending order, page by page. Plural-only — prefix scan is inherently
   * plural.
   */
  scanVertices: {
    path: "/graph.v1.LanternService/ScanVertices" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ScanVerticesRequest): Buffer => Buffer.from(ScanVerticesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ScanVerticesRequest => ScanVerticesRequest.decode(value),
    responseSerialize: (value: ScanVerticesResponse): Buffer =>
      Buffer.from(ScanVerticesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ScanVerticesResponse => ScanVerticesResponse.decode(value),
  },
  /**
   * CountVerticesByPrefix returns the number of live vertices whose key
   * starts with the given prefix.
   */
  countVerticesByPrefix: {
    path: "/graph.v1.LanternService/CountVerticesByPrefix" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CountVerticesByPrefixRequest): Buffer =>
      Buffer.from(CountVerticesByPrefixRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CountVerticesByPrefixRequest => CountVerticesByPrefixRequest.decode(value),
    responseSerialize: (value: CountVerticesByPrefixResponse): Buffer =>
      Buffer.from(CountVerticesByPrefixResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CountVerticesByPrefixResponse => CountVerticesByPrefixResponse.decode(value),
  },
  /**
   * DeleteVerticesByPrefix deletes up to `limit` vertices whose key starts
   * with the given prefix. Pass `dry_run = true` to preview the count
   * without mutating state.
   */
  deleteVerticesByPrefix: {
    path: "/graph.v1.LanternService/DeleteVerticesByPrefix" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteVerticesByPrefixRequest): Buffer =>
      Buffer.from(DeleteVerticesByPrefixRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteVerticesByPrefixRequest => DeleteVerticesByPrefixRequest.decode(value),
    responseSerialize: (value: DeleteVerticesByPrefixResponse): Buffer =>
      Buffer.from(DeleteVerticesByPrefixResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteVerticesByPrefixResponse =>
      DeleteVerticesByPrefixResponse.decode(value),
  },
  getEdge: {
    path: "/graph.v1.LanternService/GetEdge" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetEdgeRequest): Buffer => Buffer.from(GetEdgeRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetEdgeRequest => GetEdgeRequest.decode(value),
    responseSerialize: (value: GetEdgeResponse): Buffer => Buffer.from(GetEdgeResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetEdgeResponse => GetEdgeResponse.decode(value),
  },
  /** GetEdges reads several edges in one round trip. */
  getEdges: {
    path: "/graph.v1.LanternService/GetEdges" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetEdgesRequest): Buffer => Buffer.from(GetEdgesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetEdgesRequest => GetEdgesRequest.decode(value),
    responseSerialize: (value: GetEdgesResponse): Buffer => Buffer.from(GetEdgesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetEdgesResponse => GetEdgesResponse.decode(value),
  },
  /** AddEdge is non-idempotent (accumulates weight). Thin facade over AddEdges. */
  addEdge: {
    path: "/graph.v1.LanternService/AddEdge" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AddEdgeRequest): Buffer => Buffer.from(AddEdgeRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AddEdgeRequest => AddEdgeRequest.decode(value),
    responseSerialize: (value: AddEdgeResponse): Buffer => Buffer.from(AddEdgeResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AddEdgeResponse => AddEdgeResponse.decode(value),
  },
  /** AddEdges is non-idempotent (accumulates weight). POST per REST conventions. */
  addEdges: {
    path: "/graph.v1.LanternService/AddEdges" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AddEdgesRequest): Buffer => Buffer.from(AddEdgesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AddEdgesRequest => AddEdgesRequest.decode(value),
    responseSerialize: (value: AddEdgesResponse): Buffer => Buffer.from(AddEdgesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AddEdgesResponse => AddEdgesResponse.decode(value),
  },
  /** PutEdge is idempotent (replaces weight). Thin facade over PutEdges. */
  putEdge: {
    path: "/graph.v1.LanternService/PutEdge" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PutEdgeRequest): Buffer => Buffer.from(PutEdgeRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PutEdgeRequest => PutEdgeRequest.decode(value),
    responseSerialize: (value: PutEdgeResponse): Buffer => Buffer.from(PutEdgeResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PutEdgeResponse => PutEdgeResponse.decode(value),
  },
  /** PutEdges is idempotent (replaces weight). PUT per REST conventions. */
  putEdges: {
    path: "/graph.v1.LanternService/PutEdges" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PutEdgesRequest): Buffer => Buffer.from(PutEdgesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PutEdgesRequest => PutEdgesRequest.decode(value),
    responseSerialize: (value: PutEdgesResponse): Buffer => Buffer.from(PutEdgesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PutEdgesResponse => PutEdgesResponse.decode(value),
  },
  deleteEdge: {
    path: "/graph.v1.LanternService/DeleteEdge" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteEdgeRequest): Buffer => Buffer.from(DeleteEdgeRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteEdgeRequest => DeleteEdgeRequest.decode(value),
    responseSerialize: (value: DeleteEdgeResponse): Buffer => Buffer.from(DeleteEdgeResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteEdgeResponse => DeleteEdgeResponse.decode(value),
  },
  /** DeleteEdges removes several edges in one round trip. */
  deleteEdges: {
    path: "/graph.v1.LanternService/DeleteEdges" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteEdgesRequest): Buffer => Buffer.from(DeleteEdgesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteEdgesRequest => DeleteEdgesRequest.decode(value),
    responseSerialize: (value: DeleteEdgesResponse): Buffer => Buffer.from(DeleteEdgesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteEdgesResponse => DeleteEdgesResponse.decode(value),
  },
  /**
   * ScanEdges streams edges whose tail key starts with `tail_prefix` AND
   * whose head key starts with `head_prefix`, in ascending (tail, head)
   * order, page by page. Plural-only — prefix scan is inherently plural.
   */
  scanEdges: {
    path: "/graph.v1.LanternService/ScanEdges" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ScanEdgesRequest): Buffer => Buffer.from(ScanEdgesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ScanEdgesRequest => ScanEdgesRequest.decode(value),
    responseSerialize: (value: ScanEdgesResponse): Buffer => Buffer.from(ScanEdgesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ScanEdgesResponse => ScanEdgesResponse.decode(value),
  },
  /**
   * GetServerStatus returns a flat snapshot of the server's identity,
   * build info, configuration ceilings, and current live vertex/edge
   * counts. Read-only and cheap — intended for the admin UI's "Ops"
   * tab and lightweight smoke-test tooling. Auth is the caller's
   * responsibility; no PII is returned.
   */
  getServerStatus: {
    path: "/graph.v1.LanternService/GetServerStatus" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetServerStatusRequest): Buffer =>
      Buffer.from(GetServerStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetServerStatusRequest => GetServerStatusRequest.decode(value),
    responseSerialize: (value: GetServerStatusResponse): Buffer =>
      Buffer.from(GetServerStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetServerStatusResponse => GetServerStatusResponse.decode(value),
  },
  /**
   * GetReplicationStatus returns a flat snapshot of the local node's
   * outbound peer-replication state. Read-only — no peer add/remove
   * surface is exposed (see #315 out-of-scope). Cheap to call from a
   * dashboard at any cadence the operator finds useful. On
   * single-instance deployments enabled=false and peers is empty.
   */
  getReplicationStatus: {
    path: "/graph.v1.LanternService/GetReplicationStatus" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetReplicationStatusRequest): Buffer =>
      Buffer.from(GetReplicationStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetReplicationStatusRequest => GetReplicationStatusRequest.decode(value),
    responseSerialize: (value: GetReplicationStatusResponse): Buffer =>
      Buffer.from(GetReplicationStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetReplicationStatusResponse => GetReplicationStatusResponse.decode(value),
  },
} as const;

export interface LanternServiceServer extends UntypedServiceImplementation {
  illuminate: handleUnaryCall<IlluminateRequest, IlluminateResponse>;
  getVertex: handleUnaryCall<GetVertexRequest, GetVertexResponse>;
  /** GetVertices reads several vertices in one round trip. */
  getVertices: handleUnaryCall<GetVerticesRequest, GetVerticesResponse>;
  /** PutVertex writes a single vertex. Thin facade over PutVertices. */
  putVertex: handleUnaryCall<PutVertexRequest, PutVertexResponse>;
  putVertices: handleUnaryCall<PutVerticesRequest, PutVerticesResponse>;
  deleteVertex: handleUnaryCall<DeleteVertexRequest, DeleteVertexResponse>;
  /** DeleteVertices removes several vertices in one round trip. */
  deleteVertices: handleUnaryCall<DeleteVerticesRequest, DeleteVerticesResponse>;
  /**
   * ScanVertices streams vertices whose key starts with the given prefix in
   * ascending order, page by page. Plural-only — prefix scan is inherently
   * plural.
   */
  scanVertices: handleUnaryCall<ScanVerticesRequest, ScanVerticesResponse>;
  /**
   * CountVerticesByPrefix returns the number of live vertices whose key
   * starts with the given prefix.
   */
  countVerticesByPrefix: handleUnaryCall<CountVerticesByPrefixRequest, CountVerticesByPrefixResponse>;
  /**
   * DeleteVerticesByPrefix deletes up to `limit` vertices whose key starts
   * with the given prefix. Pass `dry_run = true` to preview the count
   * without mutating state.
   */
  deleteVerticesByPrefix: handleUnaryCall<DeleteVerticesByPrefixRequest, DeleteVerticesByPrefixResponse>;
  getEdge: handleUnaryCall<GetEdgeRequest, GetEdgeResponse>;
  /** GetEdges reads several edges in one round trip. */
  getEdges: handleUnaryCall<GetEdgesRequest, GetEdgesResponse>;
  /** AddEdge is non-idempotent (accumulates weight). Thin facade over AddEdges. */
  addEdge: handleUnaryCall<AddEdgeRequest, AddEdgeResponse>;
  /** AddEdges is non-idempotent (accumulates weight). POST per REST conventions. */
  addEdges: handleUnaryCall<AddEdgesRequest, AddEdgesResponse>;
  /** PutEdge is idempotent (replaces weight). Thin facade over PutEdges. */
  putEdge: handleUnaryCall<PutEdgeRequest, PutEdgeResponse>;
  /** PutEdges is idempotent (replaces weight). PUT per REST conventions. */
  putEdges: handleUnaryCall<PutEdgesRequest, PutEdgesResponse>;
  deleteEdge: handleUnaryCall<DeleteEdgeRequest, DeleteEdgeResponse>;
  /** DeleteEdges removes several edges in one round trip. */
  deleteEdges: handleUnaryCall<DeleteEdgesRequest, DeleteEdgesResponse>;
  /**
   * ScanEdges streams edges whose tail key starts with `tail_prefix` AND
   * whose head key starts with `head_prefix`, in ascending (tail, head)
   * order, page by page. Plural-only — prefix scan is inherently plural.
   */
  scanEdges: handleUnaryCall<ScanEdgesRequest, ScanEdgesResponse>;
  /**
   * GetServerStatus returns a flat snapshot of the server's identity,
   * build info, configuration ceilings, and current live vertex/edge
   * counts. Read-only and cheap — intended for the admin UI's "Ops"
   * tab and lightweight smoke-test tooling. Auth is the caller's
   * responsibility; no PII is returned.
   */
  getServerStatus: handleUnaryCall<GetServerStatusRequest, GetServerStatusResponse>;
  /**
   * GetReplicationStatus returns a flat snapshot of the local node's
   * outbound peer-replication state. Read-only — no peer add/remove
   * surface is exposed (see #315 out-of-scope). Cheap to call from a
   * dashboard at any cadence the operator finds useful. On
   * single-instance deployments enabled=false and peers is empty.
   */
  getReplicationStatus: handleUnaryCall<GetReplicationStatusRequest, GetReplicationStatusResponse>;
}

export interface LanternServiceClient extends Client {
  illuminate(
    request: IlluminateRequest,
    callback: (error: ServiceError | null, response: IlluminateResponse) => void,
  ): ClientUnaryCall;
  illuminate(
    request: IlluminateRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: IlluminateResponse) => void,
  ): ClientUnaryCall;
  illuminate(
    request: IlluminateRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: IlluminateResponse) => void,
  ): ClientUnaryCall;
  getVertex(
    request: GetVertexRequest,
    callback: (error: ServiceError | null, response: GetVertexResponse) => void,
  ): ClientUnaryCall;
  getVertex(
    request: GetVertexRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetVertexResponse) => void,
  ): ClientUnaryCall;
  getVertex(
    request: GetVertexRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetVertexResponse) => void,
  ): ClientUnaryCall;
  /** GetVertices reads several vertices in one round trip. */
  getVertices(
    request: GetVerticesRequest,
    callback: (error: ServiceError | null, response: GetVerticesResponse) => void,
  ): ClientUnaryCall;
  getVertices(
    request: GetVerticesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetVerticesResponse) => void,
  ): ClientUnaryCall;
  getVertices(
    request: GetVerticesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetVerticesResponse) => void,
  ): ClientUnaryCall;
  /** PutVertex writes a single vertex. Thin facade over PutVertices. */
  putVertex(
    request: PutVertexRequest,
    callback: (error: ServiceError | null, response: PutVertexResponse) => void,
  ): ClientUnaryCall;
  putVertex(
    request: PutVertexRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PutVertexResponse) => void,
  ): ClientUnaryCall;
  putVertex(
    request: PutVertexRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PutVertexResponse) => void,
  ): ClientUnaryCall;
  putVertices(
    request: PutVerticesRequest,
    callback: (error: ServiceError | null, response: PutVerticesResponse) => void,
  ): ClientUnaryCall;
  putVertices(
    request: PutVerticesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PutVerticesResponse) => void,
  ): ClientUnaryCall;
  putVertices(
    request: PutVerticesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PutVerticesResponse) => void,
  ): ClientUnaryCall;
  deleteVertex(
    request: DeleteVertexRequest,
    callback: (error: ServiceError | null, response: DeleteVertexResponse) => void,
  ): ClientUnaryCall;
  deleteVertex(
    request: DeleteVertexRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteVertexResponse) => void,
  ): ClientUnaryCall;
  deleteVertex(
    request: DeleteVertexRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteVertexResponse) => void,
  ): ClientUnaryCall;
  /** DeleteVertices removes several vertices in one round trip. */
  deleteVertices(
    request: DeleteVerticesRequest,
    callback: (error: ServiceError | null, response: DeleteVerticesResponse) => void,
  ): ClientUnaryCall;
  deleteVertices(
    request: DeleteVerticesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteVerticesResponse) => void,
  ): ClientUnaryCall;
  deleteVertices(
    request: DeleteVerticesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteVerticesResponse) => void,
  ): ClientUnaryCall;
  /**
   * ScanVertices streams vertices whose key starts with the given prefix in
   * ascending order, page by page. Plural-only — prefix scan is inherently
   * plural.
   */
  scanVertices(
    request: ScanVerticesRequest,
    callback: (error: ServiceError | null, response: ScanVerticesResponse) => void,
  ): ClientUnaryCall;
  scanVertices(
    request: ScanVerticesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ScanVerticesResponse) => void,
  ): ClientUnaryCall;
  scanVertices(
    request: ScanVerticesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ScanVerticesResponse) => void,
  ): ClientUnaryCall;
  /**
   * CountVerticesByPrefix returns the number of live vertices whose key
   * starts with the given prefix.
   */
  countVerticesByPrefix(
    request: CountVerticesByPrefixRequest,
    callback: (error: ServiceError | null, response: CountVerticesByPrefixResponse) => void,
  ): ClientUnaryCall;
  countVerticesByPrefix(
    request: CountVerticesByPrefixRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CountVerticesByPrefixResponse) => void,
  ): ClientUnaryCall;
  countVerticesByPrefix(
    request: CountVerticesByPrefixRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CountVerticesByPrefixResponse) => void,
  ): ClientUnaryCall;
  /**
   * DeleteVerticesByPrefix deletes up to `limit` vertices whose key starts
   * with the given prefix. Pass `dry_run = true` to preview the count
   * without mutating state.
   */
  deleteVerticesByPrefix(
    request: DeleteVerticesByPrefixRequest,
    callback: (error: ServiceError | null, response: DeleteVerticesByPrefixResponse) => void,
  ): ClientUnaryCall;
  deleteVerticesByPrefix(
    request: DeleteVerticesByPrefixRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteVerticesByPrefixResponse) => void,
  ): ClientUnaryCall;
  deleteVerticesByPrefix(
    request: DeleteVerticesByPrefixRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteVerticesByPrefixResponse) => void,
  ): ClientUnaryCall;
  getEdge(
    request: GetEdgeRequest,
    callback: (error: ServiceError | null, response: GetEdgeResponse) => void,
  ): ClientUnaryCall;
  getEdge(
    request: GetEdgeRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetEdgeResponse) => void,
  ): ClientUnaryCall;
  getEdge(
    request: GetEdgeRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetEdgeResponse) => void,
  ): ClientUnaryCall;
  /** GetEdges reads several edges in one round trip. */
  getEdges(
    request: GetEdgesRequest,
    callback: (error: ServiceError | null, response: GetEdgesResponse) => void,
  ): ClientUnaryCall;
  getEdges(
    request: GetEdgesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetEdgesResponse) => void,
  ): ClientUnaryCall;
  getEdges(
    request: GetEdgesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetEdgesResponse) => void,
  ): ClientUnaryCall;
  /** AddEdge is non-idempotent (accumulates weight). Thin facade over AddEdges. */
  addEdge(
    request: AddEdgeRequest,
    callback: (error: ServiceError | null, response: AddEdgeResponse) => void,
  ): ClientUnaryCall;
  addEdge(
    request: AddEdgeRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AddEdgeResponse) => void,
  ): ClientUnaryCall;
  addEdge(
    request: AddEdgeRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AddEdgeResponse) => void,
  ): ClientUnaryCall;
  /** AddEdges is non-idempotent (accumulates weight). POST per REST conventions. */
  addEdges(
    request: AddEdgesRequest,
    callback: (error: ServiceError | null, response: AddEdgesResponse) => void,
  ): ClientUnaryCall;
  addEdges(
    request: AddEdgesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AddEdgesResponse) => void,
  ): ClientUnaryCall;
  addEdges(
    request: AddEdgesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AddEdgesResponse) => void,
  ): ClientUnaryCall;
  /** PutEdge is idempotent (replaces weight). Thin facade over PutEdges. */
  putEdge(
    request: PutEdgeRequest,
    callback: (error: ServiceError | null, response: PutEdgeResponse) => void,
  ): ClientUnaryCall;
  putEdge(
    request: PutEdgeRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PutEdgeResponse) => void,
  ): ClientUnaryCall;
  putEdge(
    request: PutEdgeRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PutEdgeResponse) => void,
  ): ClientUnaryCall;
  /** PutEdges is idempotent (replaces weight). PUT per REST conventions. */
  putEdges(
    request: PutEdgesRequest,
    callback: (error: ServiceError | null, response: PutEdgesResponse) => void,
  ): ClientUnaryCall;
  putEdges(
    request: PutEdgesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PutEdgesResponse) => void,
  ): ClientUnaryCall;
  putEdges(
    request: PutEdgesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PutEdgesResponse) => void,
  ): ClientUnaryCall;
  deleteEdge(
    request: DeleteEdgeRequest,
    callback: (error: ServiceError | null, response: DeleteEdgeResponse) => void,
  ): ClientUnaryCall;
  deleteEdge(
    request: DeleteEdgeRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteEdgeResponse) => void,
  ): ClientUnaryCall;
  deleteEdge(
    request: DeleteEdgeRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteEdgeResponse) => void,
  ): ClientUnaryCall;
  /** DeleteEdges removes several edges in one round trip. */
  deleteEdges(
    request: DeleteEdgesRequest,
    callback: (error: ServiceError | null, response: DeleteEdgesResponse) => void,
  ): ClientUnaryCall;
  deleteEdges(
    request: DeleteEdgesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteEdgesResponse) => void,
  ): ClientUnaryCall;
  deleteEdges(
    request: DeleteEdgesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteEdgesResponse) => void,
  ): ClientUnaryCall;
  /**
   * ScanEdges streams edges whose tail key starts with `tail_prefix` AND
   * whose head key starts with `head_prefix`, in ascending (tail, head)
   * order, page by page. Plural-only — prefix scan is inherently plural.
   */
  scanEdges(
    request: ScanEdgesRequest,
    callback: (error: ServiceError | null, response: ScanEdgesResponse) => void,
  ): ClientUnaryCall;
  scanEdges(
    request: ScanEdgesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ScanEdgesResponse) => void,
  ): ClientUnaryCall;
  scanEdges(
    request: ScanEdgesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ScanEdgesResponse) => void,
  ): ClientUnaryCall;
  /**
   * GetServerStatus returns a flat snapshot of the server's identity,
   * build info, configuration ceilings, and current live vertex/edge
   * counts. Read-only and cheap — intended for the admin UI's "Ops"
   * tab and lightweight smoke-test tooling. Auth is the caller's
   * responsibility; no PII is returned.
   */
  getServerStatus(
    request: GetServerStatusRequest,
    callback: (error: ServiceError | null, response: GetServerStatusResponse) => void,
  ): ClientUnaryCall;
  getServerStatus(
    request: GetServerStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetServerStatusResponse) => void,
  ): ClientUnaryCall;
  getServerStatus(
    request: GetServerStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetServerStatusResponse) => void,
  ): ClientUnaryCall;
  /**
   * GetReplicationStatus returns a flat snapshot of the local node's
   * outbound peer-replication state. Read-only — no peer add/remove
   * surface is exposed (see #315 out-of-scope). Cheap to call from a
   * dashboard at any cadence the operator finds useful. On
   * single-instance deployments enabled=false and peers is empty.
   */
  getReplicationStatus(
    request: GetReplicationStatusRequest,
    callback: (error: ServiceError | null, response: GetReplicationStatusResponse) => void,
  ): ClientUnaryCall;
  getReplicationStatus(
    request: GetReplicationStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetReplicationStatusResponse) => void,
  ): ClientUnaryCall;
  getReplicationStatus(
    request: GetReplicationStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetReplicationStatusResponse) => void,
  ): ClientUnaryCall;
}

export const LanternServiceClient = makeGenericClientConstructor(
  LanternServiceService,
  "graph.v1.LanternService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): LanternServiceClient;
  service: typeof LanternServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
